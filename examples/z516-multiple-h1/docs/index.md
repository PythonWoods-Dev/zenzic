# First Title

Introduction content.

# Second Title

This page triggers Z516 because it contains more than one H1 heading. A
document should have exactly one top-level title, since most renderers and
search engines treat the first H1 as the canonical page title for indexing.
A second H1 further down the page creates ambiguity about which heading
actually represents the document.
