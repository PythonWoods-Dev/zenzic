# API Reference

API details here.

# Overview

Overview details here. Enforcing a strict heading order helps generated
documentation sites present sections in the sequence readers expect, rather
than the order individual contributors happened to write them in, which
keeps navigation predictable across every page in the set and reduces
confusion when readers jump between related documents.
