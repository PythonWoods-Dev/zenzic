# Setup Guide

This page closes the loop. Its only outgoing link points back at the page that
sent the reader here, which is the shape `Z106` reports: a chain of links whose
every exit leads back into the chain it came from.

Nothing here is broken. Both targets resolve, both files exist, and every hop
is valid — which is precisely why a link checker alone would report nothing at
all and the reader would still be going in circles.

- [Getting started](index.md)
