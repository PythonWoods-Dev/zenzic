# Installation Guide

This document lacks an Overview heading. Requiring a standard heading like "Overview" across every page in a documentation set gives readers a predictable entry point regardless of which specific page they land on first. That holds whether they arrived via search or a direct shared link from a colleague.
