# Z615 — Forbidden URL Scheme

Gallery example demonstrating **Z615 FORBIDDEN_URL_SCHEME**.
When `[policies].required_url_schemes` is populated, links using non-whitelisted protocols (such as http://) emit a Z615 finding.
