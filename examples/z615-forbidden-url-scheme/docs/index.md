# Welcome

Check out [insecure site](http://example.com/docs).

Restricting links to a set of required URL schemes keeps documentation
from directing readers toward plaintext connections that leak query
parameters and cookies in transit. See the
[secure site](https://example.com/docs) for the same content served over
an approved scheme instead of the insecure one shown above.
