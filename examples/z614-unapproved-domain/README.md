# Z614 — Unapproved Domain Reference

Gallery example demonstrating **Z614 UNAPPROVED_DOMAIN_REFERENCE**.
When `[policies].allowed_external_domains` is populated, external links referencing domains outside the whitelist emit a Z614 finding.
