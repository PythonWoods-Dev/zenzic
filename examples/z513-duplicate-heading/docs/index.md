# Duplicate Heading Example

## Configuration

Section about configuration.

## Configuration

Duplicate section about configuration. Repeating an identical heading text
twice in one document confuses anchor links and table-of-contents tooling,
since both sections resolve to the same generated fragment identifier and
a reader following an in-page anchor can only ever land on the first one,
never the second.
