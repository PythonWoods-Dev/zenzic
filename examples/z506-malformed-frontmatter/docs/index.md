--
title: Deployment Notes
author: Platform Team
--

# Deployment Notes

The frontmatter above opens and closes with two hyphens instead of three. A
reader sees something that looks like metadata; the parser sees prose, and every
key in that block is invisible to any rule that depends on frontmatter.
