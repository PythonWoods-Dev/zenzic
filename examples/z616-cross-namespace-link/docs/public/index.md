# Public Index

For secret details, see [Internal Secret Spec](../internal/secret.md).

This page belongs to the `docs/public` namespace, which the project policy
restricts from linking into `docs/internal`. The link above is the
intentional policy violation that Z616 is designed to catch during a scan,
before it ever reaches a production deployment.
