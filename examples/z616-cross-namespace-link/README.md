# Z616 — Cross-Namespace Link Forbidden

Gallery example demonstrating **Z616 CROSS_NAMESPACE_LINK_FORBIDDEN**.
When `[policies].cross_namespace_restrictions` is configured, links originating from a restricted source namespace (e.g., `docs/public`) targeting a forbidden namespace (e.g., `docs/internal`) emit a Z616 finding.
