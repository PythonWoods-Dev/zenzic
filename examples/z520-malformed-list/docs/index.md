# Malformed List Example

Here are the components:
Parser module;
Mutator pipeline;
Scanner engine;

Formatting a series of items as plain semicolon-terminated lines instead of
a proper Markdown list prevents renderers from applying list styling. It
also makes the structure harder for readers to scan quickly, especially
when the list grows beyond a handful of entries.
