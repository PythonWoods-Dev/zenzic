# Table Enum Example

| Feature | Status |
| :--- | :--- |
| Engine | unknown_status |

Constraining a status column to a fixed enumeration keeps documentation
tables consistent, and lets tooling validate that every row reflects one of
the lifecycle stages a project actually recognizes, rather than an ad hoc
label a single contributor invented.
