<!-- SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- markdownlint-disable MD024 -->
# Changelog

All notable changes to Zenzic are documented here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versions follow [Semantic Versioning](https://semver.org/).

---

## [Unreleased]

### Added

- **`is_likely_placeholder` — Deterministic Documented-Example Detection on `Z201`/`Z204` Findings**:
  - `SecurityFinding`/`Finding` gain a new `is_likely_placeholder: bool` field, set by a fixed, rule-based classifier (a marker-substring lookup — `"EXAMPLE"`, `"PLACEHOLDER"`, `"YOUR_API_KEY"`, etc. — plus an 8+ repeated-character-run check) applied at every credential/forbidden-term detection site. Deliberately **not** a confidence score: Tier-0 Invariant #1 (Determinism & Pure Functions) forbids probabilistic logic in the Core, so this is a fixed lookup, not a heuristic. Never suppresses or downgrades the finding — `Z201`/`Z204` remain non-suppressible, Exit 2 findings regardless of this flag; it is a display hint only. Surfaces as a `[LIKELY PLACEHOLDER]` tag next to the existing `SECURITY BREACH DETECTED`/`POLICY VIOLATION DETECTED` panel header in text output, and as an `is_likely_placeholder: true` SARIF result property when set (mirrors the existing `security-severity` property's pattern).
- **`--config PATH` — Explicit Config File Override on `check all`, `score`, `diff`, `audit`**:
  - `ZenzicConfig.load()` gained a new `config_file` override parameter (priority 0, ahead of the normal `.zenzic.toml` → `pyproject.toml` → defaults discovery chain). When given, that exact file is loaded — it does not have to live under the repository root — and still layers `.zenzic.local.toml` on top like the two discovered paths. A missing or malformed override raises immediately rather than silently falling back to discovery. Threaded through as `--config PATH` on the four commands `zenzic-action` actually invokes (`check all`, `score`, `diff`, `audit`); the other CLI commands (`check links`, `fix`, `guard`, `clean`, etc.) do not yet have the flag — closing that gap is tracked separately. This closes a long-standing gap where `zenzic-action`'s `config-file` input was fully documented (in `action.yml` and its README) but silently did nothing, because core never had a corresponding flag to receive it.
- **Specification-Driven Development (SDD) Rule Suite**:
  - `Z521` (`REQUIRED_TABLE_COLUMN`): Enforces mandatory table column headers declared under `[policies.required_table_columns]` (globally `*` or section-scoped). Penalty: 2.0 pts (Content).
  - `Z522` (`TABLE_CELL_ENUM`): Restricts table cell values to predefined enum sets declared under `[policies.table_cell_enums]` with case-insensitive matching and precise data-row line reporting. Penalty: 2.0 pts (Content).
  - `Z523` (`HEADING_ORDER_VIOLATION`): Enforces ordered document section templates declared in `[policies].required_heading_order`. Penalty: 2.0 pts (Content).
  - `Z412` (`TRACEABILITY_BROKEN`): Enforces inbound graph link coverage from designated source namespaces to target specification documents declared in `[policies.traceability_targets]`. Penalty: 4.0 pts (Navigation).
- **GFM Table AST & Lossless Parser**:
  - Extended AST in `zenzic.core.ast` with `TableNode`, `TableRow`, and `TableCell` dataclasses fully compliant with the multiprocessing Plugin Pickling Contract.
  - Implemented $O(N)$ native GFM table parser in `zenzic.core.parser` with escaped pipe `\|` support, inline code span protection, and lossless byte-for-byte serialization.
- **Policy-as-Code Configuration Expansion**:
  - Added `required_table_columns`, `table_cell_enums`, `required_heading_order`, and `traceability_targets` to `PoliciesConfig` in `zenzic.models.config`.
- **Smart Build Engine Disambiguation**:
  - Implemented lightweight, allocation-free string inspection in `discover_engine()` (`src/zenzic/core/adapters/_factory.py`) to automatically identify Zensical projects configured via MkDocs YAML (`theme: zensical`) without YAML parser overhead or false positives in nav/comments. Backed by `tests/test_engine_discovery.py`.
- **DQS Mathematical Transparency & CLI `--breakdown`**:
  - Added `--breakdown` flag to `zenzic score` displaying a fully exploded scoring ledger with category deductions, Gravity Cap calculations, and technical debt math. Showcased in root `README.md`.
- **Architectural Decision Records (ADR-089, ADR-093)**:
  - Added [ADR 089](docs/developers/explanation/adr-vault/records/adr-089-ecosystem-wide-immutable-sha-pinning.md) defining *Ecosystem-Wide Immutable SHA Pinning* — formalizing a policy already actively enforced across `zenzic`, `zenzic-action`, and `zenzic-vscode` CI/pre-commit configuration, previously only cited informally (no corresponding vault entry existed).
  - Added [ADR 093](docs/developers/explanation/adr-vault/records/adr-093-topological-suppression-non-inline.md) defining the *Topological Suppression Non-Inline Invariant & LSP UX Determinism*.
- **Tested Compatibility Matrix**:
  - Added `docs/reference/compatibility.md`, the canonical reference for which MkDocs/Material for MkDocs/Zensical version Zenzic's adapters are verified against, the verification method, and the last-verified date per engine — registered in `mkdocs.yml` nav, linked from `README.md` and `docs/how-to/configure-adapter.md`. `docs/reference/engines.md`'s prior "Supported Engine Versions" table (accurate but undated, with no stated verification method) now points to this page instead of duplicating it.
- **Documentation & Rule Cards**:
  - Added rule cards `docs/rules/Z521.md`, `docs/rules/Z522.md`, `docs/rules/Z523.md`, `docs/rules/Z412.md`, registered in `mkdocs.yml` and `docs/reference/finding-codes.md`.
  - Added launch blog post `docs/blog/posts/2026-08-22-zenzic-v0310-specification-driven-development.md`.
  - Added interactive lab scenarios `z521`, `z522`, `z523`, `z412` in `examples/` and registered them in `zenzic lab`.
- **`docs/developers/how-to/contribute/propose-a-custom-rule.md` — Documents the Previously-Undocumented `custom_rule_proposal.yml` GitHub Issue Template**:
  - A full bidirectional audit of `.github/ISSUE_TEMPLATE/*` against `docs/developers/how-to/contribute/` found `custom_rule_proposal.yml` (propose a new Z-Code rule) had no corresponding documentation page or link anywhere in `docs/`, unlike `bug_report.yml` and `feature_request.yml`. Added a new page describing its real fields (flaw description, failing/passing Markdown examples, proposed Z-Code name), registered in nav and linked from `contribute/index.md`'s issue-creation card grid. The same audit found `good_first_issue.yml` (claim an easy task) similarly absent from `contribute/index.md` — added a one-line mention rather than a full page, since the template is self-explanatory and lightweight. `gate-bypass-postmortem.md` was confirmed intentionally undocumented in contributor-facing docs — it's an internal maintainer-only Break-Glass incident template, not a contribution path. `SECURITY.md` documented the private vulnerability-reporting channels (Security Advisories, email) but never mentioned that `security_vulnerability.yml` also exists as a public template for lower-severity, non-exploitable reports — added a short note pointing to it.
- **`docs/developers/explanation/adr-vault/records/adr-031-ssot-code-definitions-and-gate-paradox.md` — Retroactively Drafted, per the ADR-089 Precedent**:
  - `ADR-031` had been a phantom citation for the whole session (5 occurrences across `codes.py`, `scorer.py` ×2 comments, this blog post, 2 example READMEs, `changelogs/v0.8.md` — no vault entry ever existed), but unlike the session's 4 other phantom-ADR instances, abundant real source material already existed to draft it for real rather than just removing the citation. Drafted from that scattered material, not invented: `CodeDefinition` (`codes.py`) as the Single Source of Truth for per-code severity/penalty/category, closing the "Gate Paradox" where 3 CI-blocking codes (`Z103`/`Z111`/`Z113`) carried 0 DQS penalty before v0.8.0 — a repository could fail the CI gate while `zenzic score` reported a perfect 100/100; and the Gravity Cap, which caps the total DQS at 70 whenever any scoring category's contribution reaches 0.00. Correction made mid-draft, not shipped wrong: initially conflated the flat-cost inline-suppression model (every suppression costs 1 pt, no free allowance) into this same ADR, since the same blog post narrates both as "two things v0.8.0 changed" — but the real source code cites that model as a *separate* ADR, `ADR-061` (`scorer.py` ×3, `api-json.md`), confirmed by direct grep before finalizing. Scoped this record to the Single Source of Truth and Gravity Cap only; `ADR-061` is logged as a 6th phantom-citation instance, not resolved here. Registered in `docs/developers/explanation/adr-vault/index.md` (Core Architecture Decisions table, numeric position between `ADR-022` and `ADR-075`) and `.claude/state/01-manifest.md`.

### Changed

- **`NOTICE` Now Lists Runtime Dependencies' Own Transitive Dependencies, Not Just Direct Ones**: the file already included `Shellingham` labeled "transitive dependency of Typer," but was missing 10 more packages at the same tier — `colorama` (Typer's other transitive dependency, Windows-only), 4 transitive dependencies of Pydantic (`annotated-types`, `pydantic-core`, `typing-extensions`, `typing-inspection`), 2 of Rich (`markdown-it-py`, `Pygments`), and 4 of httpx (`anyio`, `certifi`, `httpcore`, `idna`). Each new entry's license/copyright was verified directly against that package's own repository, not assumed. Entries renumbered sequentially throughout; no license text or attribution was removed, only added.
- **`NOTICE` Now Covers Tailwind CSS**: `docs/assets/css/zenzic-tailwind.min.css` is a compiled, human-run Tailwind CLI build artifact committed to the repository and served directly to documentation site visitors, but was absent from `NOTICE` entirely. Added a new "Bundled Documentation Site Assets" section (entry 29) citing Tailwind Labs, Inc.'s MIT license — verified against the project's own published `LICENSE` file. The compiled CSS already carries its own inline `/*! tailwindcss ... | MIT License | ... */` banner; this entry consolidates that attribution alongside the rest of the project's third-party notices, matching the existing precedent for self-hosted fonts.
- **`mkdocs`/`mkdocs-material` Docs Dependencies Now Have an Upper-Bound Ceiling**: `pyproject.toml`'s `docs` extra changed from `mkdocs-material>=9.0.0`/`mkdocs>=1.5.0` to `mkdocs-material>=9.0.0,<10`/`mkdocs>=1.5.0,<2`. MkDocs 2.0 is a confirmed, upcoming ground-up rewrite with no plugin system, no theme-override compatibility, and no migration path — previously nothing in the dependency declaration prevented a future `uv lock` from silently resolving into it. `docs/reference/compatibility.md` updated to describe the real, now-enforced ceiling instead of the prior "known gap" disclosure. `uv.lock`'s currently-resolved versions (`mkdocs==1.6.1`, `mkdocs-material==9.7.7`) are unaffected.
- **`mdx-asset-rationale.md` Renamed to `svg-vs-native-asset-rationale.md`**:
  - The page's content has been accurate since an earlier fix this session (a rationale for using native HTML/Mermaid over static SVG for in-page diagrams — no MDX, React, or Docusaurus relevance at all), but its filename and URL still said "mdx," left over from before that rewrite. Renamed to a filename that actually describes the content. `mkdocs.yml`'s nav entry and the one inbound cross-reference (`developers/reference/zenzic-style.md`) updated; `docs/_redirects` gained 2 new lines for the old bare canonical URL and had its 6 existing historical-variant lines (`/docs/...`, `/it/...` prefixes) retargeted to the new URL, rather than left pointing at a now-dead one. `mkdocs build --strict` clean.
- **Ecosystem Positioning Overhaul**:
  - Updated tagline and core value proposition across `zenzic`, `zenzic-action`, and `zenzic-vscode` READMEs: *"Formatters handle syntax. Prose linters handle grammar. Zenzic protects the graph—and optionally enforces lightweight editorial policy without a separate tool."*, focusing on protecting documentation graphs from AI hallucinations.
- **Mirror Law Parity Protocol (10 Mandatory Targets)**:
  - Formally expanded the Mirror Law protocol to 10 mandatory targets, elevating the `zenzic init` template (`templates.py`) and the VS Code IntelliSense JSON Schema (`zenzic.schema.json`) alongside core codes, scorer, scoring algorithm, scoring system, finding codes encyclopedia, rule cards, mkdocs nav, and lab fixtures.
- **CLI Init Configuration Template Remediation**:
  - Synchronized `zenzic init` templates (`templates.py`) across `.zenzic.toml`, `.zenzic.local.toml`, and `pyproject.toml` with all 16 Policy-as-Code fields including the 4 new SDD policies, updated GitHub Action snippets to `pythonwoods/zenzic-action@v2`, purged obsolete `Z120-Z124` polyglot comments, and added topological rule codes (`Z410`, `Z411`, `Z412`, `Z620`) to default `directory_policies` examples.

### Removed

- **4 Structural Doc Merges — Duplicated Content Consolidated to a Single Source**:
  - `docs/how-to/configuration-strategy.md` (near-empty stub) merged into `docs/reference/configuration-reference.md`'s `.zenzic.local.toml` Local Sanctuary section, which gained its one piece of genuinely unique content — the additive-merge semantics of `forbidden_patterns`/`excluded_dirs`, live-verified against `config.py`'s `_apply_local_toml`. Deleted; `mkdocs.yml` nav entry removed; `docs/_redirects` retargeted (4 historical lines + 2 new bare-canonical lines) to `/reference/configuration-reference/#local-sanctuary`; the one inbound link (`configure-privacy-gate.md`) repointed.
  - `docs/how-to/workflow-integration.md` merged into `docs/how-to/configure-ci-cd.md` as a new "Local Quality Gate" section (justfile/Makefile/standalone build-gate recipes, the cost-of-discovery table, and pre-launch/staging `ZENZIC_EXTRA_ARGS` handling) — both pages independently carried an Exit Code table/section that had already drifted out of sync once this session. Deleted; nav entry removed; `docs/_redirects` retargeted (4 historical lines + 2 new bare-canonical lines) to `/how-to/configure-ci-cd/#local-quality-gate`; `how-to/index.md`'s redundant "Pre-Commit & Git Hooks" card removed (its description never actually matched this page's real content — it described `configure-ci-cd.md`'s pre-commit-hook tab, which already existed on the merge target).
  - `docs/explanation/github-action-internals.md`'s Exit Code Contract table removed in favor of a single-source pointer to `docs/reference/zenzic-action.md#exit-codes` (the canonical, more detailed version) — the two tables had already drifted independently once this session.
  - `docs/explanation/core-mechanics.md`'s VSM and Credential Scanner sections trimmed to brief summaries pointing to `docs/explanation/architecture.md`'s more detailed, more current versions (full `Route`/`RouteMetadata` fields, F2-1/F4-1 hardening, DFA guarantee) — kept core-mechanics.md's own unique conceptual framing and its genuinely non-duplicated sections (Circular Links as Knowledge Graphs, Three-Pass Reference Pipeline, Smart Link Graph, Baseline Engine, Global Usage Tracker, Auto-Fix Engine) untouched.
  - No code behavior changed — documentation structure and content only. `mkdocs build --strict` and `zenzic check all --quiet` clean after each merge.

- **`Z000` (`UNSUPPORTED_ENGINE`) Removed — BREAKING**:
  - `Z000` is removed entirely from `codes.py`'s `CODE_DEFINITIONS`/`CODE_NAMES`/`CODE_DESCRIPTIONS`/`FROZEN_CODES`, its rule card, `finding-codes.md`, `scoring-algorithm.md`, and mkdocs nav — same precedent as the earlier `Z113`/`Z114` removal (no stub, no deprecated-but-present marker). Full git history search (`git log --all -S'code="Z000"'`, `-S"UnsupportedEngine"`) confirmed `Z000` never had live emission logic at any point in this project's history — the commit that first registered it in `CODE_NAMES`/`CODE_DESCRIPTIONS`/`CODE_SARIF_LEVELS` (`a7aa0ef`, v0.7.1) says explicitly it was "already documented in docstring and finding-codes.mdx" and only completed catalog bookkeeping, never added a check. The scenario its docs described (an unsupported `engine` value in `.zenzic.toml`) has always been — and still is — handled by `Z001` (`CORE_CONFIG_STRUCTURE`) via Pydantic's `Literal[...]` field validation; live-verified unchanged before and after this removal. `docs/reference/engines.md` corrected in the same pass — it wrongly claimed an unsupported engine value emits `Z000`. The Z0xx category (previously "Migration & Compatibility," now containing only `Z001`) is renamed "Configuration Guard" to match its sole remaining member and the label `scoring-algorithm.md` already used for it. `docs/_redirects`: `Z000`'s URLs (historical `/docs/`-prefixed variants and the bare current path) now redirect to `/rules/Z001/` rather than the generic rules index, since `Z001` is the code that actually handles the scenario `Z000`'s docs described. Any external tooling, saved baseline, or custom rule configuration referencing `Z000` by name should be updated — this identifier no longer exists in the registry.
- **`--strict`/`-s` Removed from `zenzic score` and `zenzic diff` — BREAKING**:
  - Both commands declared a `--strict`/`-s` option that was parsed but never read again in either function body — both always called the score engine with the config file's `strict` setting, never the CLI flag's value. Confirmed dead by live verification: `score`/`diff` JSON output was byte-identical with and without `--strict`, even against a fixture with a broken external link. Removed rather than wired up, since external HTTP link validation already runs unconditionally on both commands (`check_external=True`, hardcoded) with no opt-out — there was no natural, non-breaking behavior to wire the flag to. `docs/reference/cli.md` corrected in the same pass: it previously (and incorrectly) documented `--strict` on `score`/`diff` as gating external link validation — that was never true. Any script currently passing `--strict`/`-s` to `zenzic score` or `zenzic diff` will now get a CLI usage error (`No such option`).
- **`Z113` (`AUTHOR_KEY_COLLISION`) and `Z114` (`LARGE_PAGINATION_SET`) — BREAKING**:
  - Both codes are removed entirely from `codes.py`'s `CODE_DEFINITIONS`/`CODE_NAMES` registry, their rule cards, `finding-codes.md`, `cli.md`, `scoring-algorithm.md`, `scoring-system.md`, and the `zenzic inspect capabilities` "Blog Integrity Guard" scanner listing. Neither code ever had live detection logic since introduction — confirmed by exhaustive grep across the engine (no emission site in `scanner.py`, `validator.py`, `rules.py`, or any adapter) — so `zenzic inspect capabilities` was presenting an always-active scanner that in fact never ran. Removed rather than retained as an unimplemented placeholder, consistent with this project's existing precedent for a fully-superseded code identity (`Z118` → `Z620`): no stub, no deprecated-but-present marker. `docs/rules/Z113.md` and `Z114.md` are deleted; `docs/_redirects` now sends both URLs (and their historical `/docs/`-prefixed crawl variants) to the `rules/` index rather than a specific successor page, since none exists. Any external tooling, saved baseline, or custom rule configuration referencing `Z113`/`Z114` by name should be updated — these identifiers no longer exist in the registry.
- **Documentation Site Pruning — 5 Pages Cut, ADR Records + Release Runbook Moved Out of Nav (Files Preserved)**:
  - **Cut entirely** (deleted, nav entries removed, `docs/_redirects` retargeted to the nearest genuine content successor rather than left as dead 301s): `developers/explanation/governance/technical-debt.md` and `lpgp.md` (governance-as-public-doc content, redirects now point to the Governance section index); `developers/explanation/tailwind-mkdocs-bridge.md` (implementation-rationale-as-doc, redirects now point to the Developer Explanation index; its blog companion's dangling "see the full technical specification" pointer removed, the blog post's own body already stands on its own); `reference/brand-system.md` and `brand-kit.md` (redirects now point to the Reference index; 2 now-fully-orphaned badge SVGs, referenced only by the deleted `brand-kit.md`, deleted alongside them — confirmed via repo-wide grep that nothing else references them). `explanation/community-index.md`'s Brand Kit card and `reference/index.md`'s 2 dangling table rows removed in the same pass.
  - **Moved out of public nav, files preserved** (versioned reference material, link-checked normally, readable via GitHub): all 20 individual ADR record pages and `developers/how-to/release-governance-protocol.md`. Applied via a new `[governance.directory_policies]` exemption for `Z402` (`ORPHAN_PAGE`) — a 0-DQS-debt "strategic exemption," not a suppression of a real defect, since nav-absence was never a defect for this category. Discovered mid-execution that `Z103` (`ORPHAN_LINK`, incoming-link nav-reachability) also needed the same treatment for every file that legitimately cites one of these pages — extended to the same directory/file set plus `adr-vault/index.md`, `governance/licensing.md`, `how-to/troubleshooting.md`, `developers/index.md`, `reference/cli-architecture.md`, `reference/compatibility.md`, and one blog post.
  - Two dangerous factual claims on now-archived ADR pages corrected in the same pass: `adr-020-mirror-law.md` no longer describes a bilingual EN/IT documentation requirement (formally dropped by ADR-022; the real Mirror Law is 10 targets, not a second language); `adr-discovery.md` no longer claims `mkdocs.yml` is "deliberately excluded" from root markers — `find_repo_root()` has always included it and `zensical.toml` as fallback markers alongside `.git`/`.zenzic.toml`.
  - `mkdocs.yml` nav labels shortened site-wide for needlessly verbose entries (redundant qualifiers like "(LSP)", "Developer" prefixes already implied by their section, "Index" suffixes on already-indexed subsections) — no URL or slug changed.
  - No code behavior changed — documentation structure and content only.
- **`docs/developers/reference/adapter-examples.md` Merged Into the Z-Code Gallery (`docs/tutorials/examples/index.md`)**:
  - The page described 5 named example projects (`broken-docs`, `i18n-standard`, `security_lab`, `standalone`, `plugin-scaffold-demo`) — live-confirmed via `ls examples/` that none of them exist anymore; the real fixture tree is entirely per-Z-code (`examples/zNNN-slug/`), already fully covered by the Z-Code Gallery's own "Quick-Run Pattern" (`zenzic lab <code>`) and "Feature-to-Example Matrix." No unique content survived the comparison, so the page was deleted rather than rewritten. `mkdocs.yml`'s nav entry removed; `developers/index.md`'s "Example Projects" card retargeted to the Z-Code Gallery; `docs/_redirects` gained 2 new lines for the bare canonical URL (previously unredirected — the page existed) and had 6 existing historical-variant lines retargeted from the deleted page to `/tutorials/examples/` rather than left pointing at a 404. Whole-file `_redirects` syntax audit re-run (0 malformed lines, 0 duplicate sources) and a full repo-wide grep confirmed no other dangling reference to the deleted page remains.
- **`docs/developers/reference/supply-chain-assurance-profile.md` Cut — Governance-as-Public-Doc Content With a Confirmed Defect, Never Actually Executed From an Earlier Triage**:
  - The page instructed running `scripts/enforce-radical-unawareness.sh` — confirmed via `find`/`test -f` that this script does not exist anywhere in the repo — and referenced a `zenzic-doc` repository name that doesn't match the real 4-repo ecosystem (`zenzic`, `zenzic-action`, `zenzic-vscode`, `zenzic-mcp`). This CUT was identified in an earlier triage but never executed pending explicit authorization; now executed. Deleted; `mkdocs.yml` nav entry removed; `developers/index.md`'s intro link and its "Supply-Chain Assurance" card both removed; `release-governance-protocol.md`'s cross-reference to the deleted page removed. `docs/_redirects`: 6 existing historical-variant lines plus 2 new bare-canonical-URL lines all retarget to `/developers/how-to/release-governance-protocol/` — the thematically closest surviving content (release/CI governance), still real and reachable even though itself nav-exempt via `directory_policies`, not a generic fallback. Whole-file `_redirects` syntax audit re-run (0 malformed lines, 0 duplicate sources) and a repo-wide grep confirmed no dangling reference remains.
- **`validate_same_page_anchors` Config Field Removed — Not a Breaking Config Change**:
  - Confirmed dead this session (see `03-priority-table.md`): declared in `models/config.py` with `default=True` and a description claiming it disables same-page anchor validation, but read nowhere in `src/zenzic/core/` — `Z102` same-page anchor validation has always run unconditionally, for every file, regardless of this setting. Removed the field entirely rather than wiring it up, since the current, stricter-than-documented always-on behavior is the intended permanent behavior. Removed from `models/config.py` (the field itself, and its listing in `_validate_no_swallowed_root_keys`'s root-key set), `zenzic config explain`'s field listing (`_config_explain.py`), both `zenzic init` scaffold templates (`templates.py`), the root `.zenzic.toml`'s own commented example line, and all live doc references (`docs/rules/Z102.md`, `docs/reference/index.md`, `docs/reference/checks.md` — whose "Same-page anchor validation" tip previously described the same wrong disabled-by-default behavior, corrected in the same pass — and `docs/reference/configuration-reference.md`'s dedicated field entry and complete-example listing). **Confirmed empirically, not assumed**: `ZenzicConfig`'s `model_config` sets no `extra` policy (Pydantic v2 default, `extra="ignore"`), and `_build_from_data()` already has a generic unrecognized-top-level-key path (`config.py:869-884`) that logs a `".zenzic.toml: unknown key '...' will be ignored."` warning and silently drops the key — live-verified via a new test that a `.zenzic.toml` still setting `validate_same_page_anchors` loads successfully post-removal, with that exact warning, rather than raising a validation error. **Not a breaking change**: no deprecation-warning shim was added on top of that, since the loader's existing generic mechanism already covers this exact case. TDD evidence: 2 new tests in `test_config.py` (`test_validate_same_page_anchors_field_removed_from_model`, `test_validate_same_page_anchors_key_in_toml_is_ignored_with_warning`), both red (`AssertionError`) against the pre-removal code, both green after.
- **`docs/how-to/use-brand-system.md` and `docs/explanation/brand-philosophy.md` Cut — Brand-Identity Content Failing the Same Test That Already Removed `brand-system.md`/`brand-kit.md`**:
  - Both pages read in full and classified as internal design-system/marketing material (a CSS-token consumption contract for the site's own components, an "A/B Palette Profile" cosmetic toggle, a lexicon/posture style guide, logo-symbolism prose, a palette-design-rationale essay) — zero operational content a third-party user or contributor would actually need. Real external comparables fetched before deciding: `eslint.org/branding/` and HashiCorp's product-logo brand page are both pure trademark/logo-usage references (naming convention, logo sizing, reference-only hex values) — no posture narrative, no symbolic-meaning essay, no design-token consumption guide; Prometheus, ruff, and ffmpeg have no dedicated brand page at all. Both pages deleted; `mkdocs.yml` nav entries removed; `how-to/index.md`'s "Brand Governance System" card removed (its own description — "Configure brand term dictionaries and eradicate obsolete product naming conventions" — didn't even match the real page content, a further confirmation the card had drifted from reality); `community-index.md`'s "Philosophy" card retargeted from `brand-philosophy.md` to `explanation/why-zenzic.md`, a real page that actually covers Zenzic's design philosophy and direction. `docs/_redirects`: 4 existing historical-variant lines for `use-brand-system` retargeted to `/how-to/add-badges/` — the real, already-existing, complete "add a build/score badge to your README" page, since `use-brand-system.md` never contained any badge-related content to begin with; 4 existing historical-variant lines for `brand-philosophy` retargeted to `/explanation/why-zenzic/`; 2 new bare-canonical-URL lines added for each deleted page. **Second-order consequence found and fixed in the same pass**: `use-brand-system.md`'s own text described its font/logo/favicon link list as existing specifically to keep those real, CSS/template-consumed theme assets out of `Z405` (`UNUSED_ASSET`) — deleting the page surfaced exactly the 30 `Z405` findings its own text predicted, live-confirmed via `zenzic check all --show-info` before the fix. Added `excluded_asset_dirs = ["overrides", "brand", "fonts"]` (preserving the pre-existing `"overrides"` default, which a bare list reassignment would otherwise have silently dropped) and `excluded_assets = ["favicon.ico"]` to the root `.zenzic.toml`, replacing the deleted page's incidental markdown-link-anchoring with the same real exclusion mechanism `configure-social-metadata.md` already documents for the same class of problem. `just check` (98/100, 0 new) and `mkdocs build --strict` both clean after the fix. Full `pytest tests/` suite unaffected (2118 passed).

### Fixed

- **Stale GitHub Org Slug (`PythonWoods/zenzic` → `PythonWoods-Dev/zenzic`) Across Live, User-Facing Files**: the repository's GitHub org was renamed at some point, but `pyproject.toml`'s `[project.urls]` (`Repository`, `Source`, `Changelog`, `Issues`, `Tracker`), `mkdocs.yml`'s `repo_url`/`repo_name`/footer link, `README.md`'s badges/clone snippets/pre-commit `repo:` examples/CI-status shields.io badge, `CONTRIBUTING.md`'s repo table and SSH clone instructions, `SECURITY.md`'s advisory links and `gh attestation verify --repo` example, and `.pre-commit-hooks.yaml`'s own usage comment all still referenced the old org slug. Functionally non-breaking today (`github.com/PythonWoods/zenzic` 301-redirects to the real org, confirmed via `curl`/`git ls-remote`/a shields.io fetch), but non-canonical and liable to break if the old slug is ever reclaimed by a different account. `pyproject.toml`'s own `Repository` field — package metadata published to PyPI — was the highest-stakes instance. Scoped to live/user-facing files only; dated changelog archives and historical blog-post snapshots deliberately left untouched, per the same archival-judgment discipline already applied elsewhere this session.
- **`SECURITY.md`'s "Supported Versions" Table Claimed a Fictional Multi-Tier Backport Policy**: stated `0.7.x` (23 minor versions behind the real current `0.30.0`) as current, with `0.6.x` getting "critical fixes only" and `< 0.6` end-of-life — verified against `git branch -a`/`git tag` that no maintenance/backport branch has ever existed in this repository (every branch is a single-track `feat/`/`fix/`/`docs/` branch off `main`; `bump-my-version` only ever advances one forward-moving version). Corrected to state the real practice: a single-track release model where only the current release receives security fixes, with the real current version number.
- **`check all`'s "Initializing environment & VSM" Progress Line Displays a Frozen Mid-Animation Spinner Frame Instead of the Finished State**: the task was created via `Progress.add_task(..., total=1, completed=1)` — already "100% done" at construction — but Rich's `Task.finished_time` (the flag `SpinnerColumn` checks to switch from an animating spinner glyph to its blank finished-state text) is only ever set inside `Progress.update()`'s completion-transition check, never inside `add_task()` itself, even when `add_task()` is called with `completed >= total`. Since nothing called `progress.update()` for this specific task afterward, its `finished_time` stayed `None` forever, so `SpinnerColumn` kept rendering whatever animation frame (`⠼`, `⠹`, etc.) happened to be current at the last refresh — every other progress line in the same command follows the established `add_task()` → do work → `update(completed=1, ...)` pattern and renders correctly. Fixed by routing the init task through that same pattern. Confirmed unrelated to the previously-fixed mutmut test-flake (`03-priority-table.md`, `V031_MUTMUT_ENV_TEST_FRAGILITY`) — that was a `CliRunner` stdout-capture timing race under concurrent mutmut workers; this is a real rendering defect reproducible in any interactive terminal run, root-caused via Rich's own source (`Task.finished`/`Progress.update`/`Progress.add_task`), not a test artifact.

- **`finding-codes.md`'s `Z301` Entry Misattributed a `markdownlint`-Specific Behavior to Prettier Too** — claimed both "Prettier or Markdownlint" could auto-remove unused link reference definitions and dangle a surviving reference. Verified against each tool's own real behavior: `markdownlint`'s `MD053` rule genuinely does this via `--fix` (confirmed via its own docs and a reported real-world incident); Prettier's markdown formatter does not — it reformats, it doesn't delete content. Corrected to name `markdownlint`'s `MD053` specifically.
- **`pull-requests.md`'s `uv`-vs-`pip` Speed Claim Was an Unquantified "Significantly Faster"** — replaced with `uv`'s own published benchmark figure ("10-100x faster than pip"), cited and linked, rather than an unsourced adjective.
- **`editor-integrations.md`'s Neovim LSP Snippet Used the Older Manual Autocmd Pattern** — replaced the `FileType` autocmd + `vim.lsp.start()` example with Neovim 0.11+'s now-recommended declarative `vim.lsp.config("zenzic", {...})` + `vim.lsp.enable("zenzic")` pair. The old pattern still worked (not a defect, just dated); field names in the new snippet (`cmd`, `filetypes`, `root_markers`) verified against Neovim's own official `:h lsp-config` help text before publishing.
- **`migrate-engines.md` Didn't Mention `pyproject.toml`'s `[tool.zenzic.build_context]` as an Equivalent to the `.zenzic.toml` Example Shown** — added a cross-link to `configuration-reference.md`'s config-priority section so readers using Track-2 (`pyproject.toml`-based) configuration aren't left assuming the Zensical-adapter switch only works via `.zenzic.toml`.
- **`structural-integrity.md`'s "Node.js Tax" Section Claimed `node_modules/` Is "~300 MB"** — the real figure for this repo's own Node dependency (`markdownlint-cli`) is roughly 10 MB, about 30x smaller. Rewrote the claim as qualitative (a second runtime plus its own dependency tree) rather than substituting a different specific number, since the real footprint varies by which Node-based tool a reader actually compares against.
- **`excluded_external_urls` Prefix Matching Was Vulnerable to Host Spoofing (CWE-20)** — a declared exclusion of `"https://trusted.com"` also matched `"https://trusted.com.evil.com/..."`, since both call sites (`_check_external_links` and `LinkValidator.register` in `validator.py`) compared prefixes via a raw `url.startswith(prefix)` instead of parsing the host. Any project relying on `excluded_external_urls` to skip HTTP validation for a specific domain could have that exclusion silently apply to an unrelated, similarly-prefixed host too. Fixed via a new shared `_url_matches_excluded_prefix()` helper requiring an exact scheme+host(+port) match (via `urllib.parse`) before falling back to the original path-prefix check, preserving the documented use case of scoping an exclusion to a specific path under a host (e.g. `https://github.com/YourOrg/YourRepo`).
- **`cli.md`'s Example SARIF Output Was Schema-Incomplete and Stale** — the 2 sample rule entries omitted `fullDescription`/`properties` (real output always includes them), used `SCREAMING_SNAKE_CASE` names instead of the real PascalCase (`FileNotFound`, `CredentialSecret`), and used a relative `helpUri` form that no real code path produces. Corrected against live-generated SARIF output, not just source reading; the adjacent prose's "helpUri points to the anchor in this reference page" claim also corrected — it points to `finding-codes.md`, a different page.
- **`docs/how-to/index.md`'s "Configuration Strategy" Card Linked To and Described `initialize-configuration.md`, a Different Page From the One Named** — retitled to "Initialize Configuration," matching what the card actually links to and describes; `configuration-strategy.md` itself is a separate, distinct page.
- **`cli.md`'s Interactive Lab "Outcome Labels" Table Was Missing 3 of 8 Real Label States** — `_status_cell()` (`_lab.py:744-759`) can also produce `INCIDENT ✓`, `INCIDENT expected — not triggered`, and `EXPECTED FAIL — nothing found`, none of which were previously documented. Table rewritten with all 8 states, verified against source and a live `zenzic lab all` run.
- **`blog/atom.xml` Was Never Actually Atom — Falsely Labeled RSS Feed, Plus a Separate `<head>` Feed-Discovery Link Bug**:
  - Investigated the directive's stated premise ("RSS/Atom XML syntax errors") directly: 3 independent checks (`xml.etree.ElementTree`, `xml.dom.minidom`, a raw-content unescaped-`&` grep) all confirmed both `blog/rss.xml` and `blog/atom.xml` were genuinely well-formed XML with zero syntax errors, contradicting the premise as stated. Dug further rather than stopping there: a direct structural/vocabulary comparison (`diff`, `md5sum`, element-tag census) showed `atom.xml` was byte-for-byte the same `<rss>`/`<channel>`/`<item>` structure as `rss.xml`, not Atom content at all — traced to `mkdocs.yml`'s `plugins.rss.feeds_filenames.rss_updated` mapping the plugin's genuinely-RSS-format "updated posts" feed to a filename that falsely implied Atom. Live-verified via 2 independent sources (`mkdocs-rss-plugin`'s own docs and its GitHub config schema) that the plugin generates **RSS 2.0 and JSON Feed 1.1 only** — it has never had any Atom output capability. Renamed the file to `blog/rss-updated.xml` (an honest name for its real format) rather than implementing Atom output that doesn't exist upstream; updated all 6 references (`mkdocs.yml` nav + `feeds_filenames`, `docs/_redirects` retarget + new redirect for the dead old URL, `docs/blog/rss.xsl` subscribe link + footer link, `docs/blog/posts/2026-04-28-welcome.md` subscribe list).
  - **Separately discovered while re-inspecting the built landing page after the rename**: `mkdocs-rss-plugin` unconditionally injects `<link rel="alternate" type="application/rss+xml">` feed-discovery tags into every page's `<head>` using its own hardcoded default filenames (`feed_rss_created.xml`, `feed_rss_updated.xml`), **ignoring** the `feeds_filenames` config override entirely — a known, documented upstream limitation (confirmed via a `squidfunk/mkdocs-material` GitHub discussion describing this exact bug and the community's standard workaround). Confirmed empirically in this repo's own build: `find site -iname "feed_rss*"` returned nothing (the hardcoded files are never written), while `grep -rl "feed_rss_created\|feed_rss_updated" site/` matched 274 files, including `404.html` and the search index. Fixed via a new MkDocs `on_post_build` hook (`hooks/fix_rss_feed_discovery_links.py`, wired in via `mkdocs.yml`'s new `hooks:` key) that sweeps every file already written to `site_dir` after the build completes and rewrites the plugin's hardcoded filenames to the real ones (`on_post_build` chosen over `on_post_page` specifically because Material's `404.html` and the search index are generated outside the normal per-page render pipeline and would otherwise be missed). Re-verified after the fix: `grep -rl "feed_rss_created\|feed_rss_updated" site/` returns 0 matches anywhere in the built site.
  - Both feeds re-validated as valid XML (`ElementTree` + `minidom`) after every change. `ruff`, `mypy`, `reuse lint`, `mkdocs build --strict`, and `zenzic check all --quiet` all clean.
- **`Z521`/`Z522`/`Z523` Added to `NON_INLINE_SUPPRESSIBLE_CODES` — Inline Suppression Was Silently Non-Functional for These Codes, Now Cleanly Refused With a Clear Message**:
  - Investigation confirmed the mechanism empirically, not assumed: `Z521` (`REQUIRED_TABLE_COLUMN`) and `Z523` (`HEADING_ORDER_VIOLATION`) both anchor to the only line an inline `<!-- zenzic:ignore -->` directive could possibly match (the table's header row / the violating heading's own line) — placing the comment there corrupts the underlying GFM table-row or heading-title parsing, silently hiding the real violation instead of suppressing it. `Z522` (`TABLE_CELL_ENUM`) anchors to a data row, which empirically tolerates the trailing comment (the finding still fires correctly) — included for consistency with its sibling table-policy codes, not because it independently reproduces the same corruption. All 3 now behave like the other 9 `NON_INLINE_SUPPRESSIBLE_CODES` (`Z401`/`Z402`/`Z404`/`Z405`/`Z406`/`Z410`-`Z412`/`Z620`): inline suppression is refused outright, and a leftover directive is reported as `Z603` with the ADR-093-specific message pointing to `directory_policies`/`per_file_ignores`, instead of a generic "does not suppress any active finding" message that gave no hint of the real cause. TDD-first: 4 new tests in `tests/test_suppressions.py`, all confirmed red before the registry change, green after. Live-verified against the original real fixture that first surfaced this: the confusing generic `Z603` message is now the accurate, actionable ADR-093 one. `docs/rules/Z521.md`/`Z522.md`/`Z523.md` and `docs/reference/finding-codes.md`'s badges and "Common causes" section updated to match (Mirror Law). Full suite (2144 passed), `ruff`/`mypy` clean, `mkdocs build --strict`/`zenzic check all` clean.
- **`docs/explanation/architecture.md`'s "Living Test Bench" Section Described a Fictional `verify-codes-parity` Nox Session and a Pre-Monorepo Local-Checkout Requirement**:
  - Investigated live before touching anything: no Nox session by this or any name exists (`noxfile.py`'s real sessions are `tests`, `lint`, `format`, `fmt`, `typecheck`, `reuse`, `security`, `mutation`, `dev`), and no CI workflow references "parity" anywhere. The underlying *intent* is real, though: `tests/test_finding_codes_reference.py` is a genuine, working structural pytest test enforcing exactly this Mirror Law guard (checking `finding-codes.md`'s Severity/Penalty/Suppressible/heading-name fields against `codes.py`) — it runs automatically as part of the standard test suite (`nox -s tests`, `just verify`, CI), just not as a dedicated named session, and checks one direction only (docs entries must match `codes.py`; not the reverse). Also corrected `just verify`'s described local-checkout requirement (`ZENZIC_CORE_PATH`/`./_zenzic_core`/`../zenzic`) — real for a pre-monorepo world where docs lived in a separate repo, but moot today since Zenzic Core lives in this same repository; confirmed `docs-build` (part of `verify`'s dependency chain) is just `mkdocs build --strict`, no external path involved.
- **`docs/reference/finding-codes.md`'s 9 Topological/Governance Codes' "Suppressible: Yes" Badges Didn't Disclose the ADR-093 Inline-vs-TOML Distinction**:
  - Live-verified the real, current `NON_INLINE_SUPPRESSIBLE_CODES` set (`codes.py`) before fixing: `Z401`, `Z402`, `Z404`, `Z405`, `Z406`, `Z410`, `Z411`, `Z412`, `Z620` — 9 codes, exact match to the original finding (confirmed `Z412`, added since this finding was first logged, is already correctly included). Each entry's badge now states the real suppression path (`.zenzic.toml` `directory_policies`/`per_file_ignores` only, not an inline comment), linking to the existing `Z603` entry, which already documented the underlying mechanism in full — avoiding repeating the explanation 9 times.
- **`docs/reference/cli.md`'s Command Specification Matrix — 10 Confirmed Undocumented/Mis-Scoped Flags Fixed, Live-Verified Against Real `--help` Output for Every Claim**:
  - `check all` was missing `--audit`/`--update-baseline`/`--baseline`/`--no-header` from its own flag table, and no `check` sub-command documented its `PATH` positional argument — added all 4 flags plus a note on the shared `PATH` argument.
  - `score` was missing its `path` positional argument and `--quiet`/`-q`.
  - `zenzic clean assets` had no flag table at all — added all 7 real arguments/flags (`path`, `--yes`/`-y`, `--dry-run`, `--quiet`/`-q`, `--engine`, `--exclude-dir`, `--include-dir`).
  - `zenzic guard scan` was missing its `paths` argument, `--quiet`, and `--no-header`.
  - `zenzic config explain` had no flag table at all — added `--path`/`-p`.
  - `--quiet`'s Global Flags entry claimed it was exclusive to `check all` — corrected to name all 4 real commands (`check all`, `score`, `guard scan`, `clean assets`).
  - `--exclude-dir`/`--include-dir`'s "and individual sub-commands" claim was false (confirmed via `check links --help`, zero matches) — corrected to name the real 2 commands (`check all`, `clean assets`).
  - `--no-header` had no dedicated Global Flags entry despite being real on 4 commands — added one.
  - The Autofix & Cleanup section's fixable-codes list documented 3 of the real 6 `Mutator` classes — added the missing `Z515` (bare URL), `Z517` (heading punctuation), `Z520` (malformed list).
  - **Also found while investigating the original "`--strict`/`--offline` availability table doesn't reflect the real per-subcommand matrix" claim**: the "Shared Execution Flags" section wasn't a wrong table, it was unfilled boilerplate scaffold text ("This section details the specifications and guidelines for Shared Execution Flags within the Zenzic ecosystem") — the same pattern already found and fixed elsewhere this session. Replaced with a real, live-verified per-command availability table for `--strict`/`--offline` across all 12 commands that could plausibly have either flag.
  - `mkdocs build --strict`, full `pytest tests/` (2140 passed), `zenzic check all` (98/100, 0 new) all clean.
- **Priority-Table Backlog Batch — 6 Small, Confirmed Docs/`.claude/`-Internal Fixes**:
  - `docs/reference/glossary.md`'s "Ghost Route" definition had the *opposite* meaning of its real, codebase-canonical usage (a URL declared-but-missing a source file, rather than the real meaning: a synthetic, no-nav route auto-generated by the build engine, e.g. a `reconfigure_material` locale entry point) — rewritten to match `rules.py`/`test_vsm.py`/`architecture.md`, which already agreed.
  - `docs/explanation/architecture.md`'s "Living Test Bench" section described the documentation site as a separate `zenzic-doc` repository — git-history-confirmed (`6573beb`, the v0.15.1 "Monorepo Unification" release) this was accurate before the merge and simply never updated after. Corrected the heading and 3 body mentions to describe this repository's own docs, not a separate repo.
  - `.claude/CLAUDE.md`: Exit Code Contract's "Clean / within tolerance" (a phrase with no corresponding mechanism anywhere in the codebase) corrected to "Clean (or `--exit-zero`)", matching the real SSoT; OPERATIONAL WORKFLOW step 7's "6-Phase Execution Report" corrected to "7-Section", matching the real REPORT FORMAT section directly below it; Rule 22 (Reader-Facing Documentation Contains No Meta-Narrative) added as Core Principle item 19 — the only rule among 18-23 missing a corresponding numbered summary item.
  - 3 pre-existing test files (`test_governance_severity_ssot_structural.py`, `test_rules_severity_fix.py`, `test_suppressions_severity_ssot_structural.py`) reformatted with `ruff format` — unrelated to any behavior change, purely formatting drift from a prior `ruff` version bump.
  - `mkdocs build --strict`, `zenzic check all` (98/100, 0 new), full `pytest tests/` all clean.
- **`Z402`/`Z410` False-Positived on a Zensical Nested `README.md` Not Explicitly Listed in Nav**:
  - `ZensicalAdapter._classify_route()` only exempted `index.md`/`README.md` from orphan detection at the docs root — a nested, unlisted `guide/README.md` fell through to the generic orphan classification, even though it's a genuine, Zensical-documented per-directory index-page convention. `MkDocsAdapter` already has the exact right precedent for this (an unlisted `README.md` is `IGNORED`, not orphan, at any nesting level — deliberately asymmetric with `index.md`, which stays a real orphan candidate since it's the page conventionally expected to be explicitly listed). Mirrored that established pattern for Zensical. Live-reproduced with a real fixture before fixing: a nested `README.md` (not in nav) produced `Z410 UNREACHABLE_GRAPH_NODE`; gone after the fix. 4 new tests in `tests/test_integrity_zensical.py` covering the unlisted/listed/no-nav-declared/`index.md`-stays-orphan cases; the 2 behavior-changing cases confirmed genuinely red before the fix. Full suite (2140 passed), `ruff`/`mypy` clean, `zenzic check all`/`mkdocs build --strict` clean.
- **`Z401 MISSING_DIRECTORY_INDEX` False-Positived on Any Zensical Nested Directory Indexed Only by `README.md`**:
  - `ZensicalAdapter.provides_index()` only checked for `index.md`, but Zensical's own documentation (`zensical.org/docs/authoring/markdown/`) confirms a page is considered an index page if its basename is `index.md` **or** `README.md`, at any directory level, not just the docs root. A real Zensical project with a nested directory indexed only by `README.md` — a genuinely valid, Zensical-documented convention — incorrectly triggered `Z401`. Live-reproduced with a real fixture and `zenzic check all` before fixing: the finding fired (`docs/guide 💡 [Z401] Directory contains Markdown files but has no index page`) on a directory containing only a `README.md`; gone after the fix, with the file's real remaining findings (`Z411`, `Z502`) unaffected. Fixed by also checking for `README.md`; precedence when both files exist in the same directory is left unmodeled since Zensical's own docs state that behavior is currently undefined upstream (`zensical/backlog#135`) — the method only needs to know whether *an* index exists, which holds either way. TDD-first: 4 new tests in `tests/test_integrity_zensical.py` (`TestProvidesIndex`), the `README.md`-only case confirmed genuinely red before the fix. Full suite (2136 passed), `ruff`/`mypy` clean, `zenzic check all`/`mkdocs build --strict` clean.
- **`Z404 CONFIG_ASSET_MISSING` and `Z405 UNUSED_ASSET` Both Silently Mishandled Zensical Projects — `ZensicalAdapter` Read `favicon`/`logo`/`extra_css`/`extra_javascript` From the Wrong TOML Table**:
  - `ZensicalAdapter` (`src/zenzic/core/adapters/_zensical.py`) read `favicon`/`logo` directly off the top-level `[project]` table, but Zensical's own real schema — live-verified against `zensical.org/docs/setup/logo-and-icons/` and the actual bootstrap `zensical.toml` shipped in Zensical's own repository — nests both fields under `[project.theme]`. **Consequence**: `Z404` could never detect a missing favicon/logo on any Zensical project that followed Zensical's own documented convention — the check silently no-op'd, not because the asset genuinely existed, but because the adapter was never looking in the right place. A second, independently-discovered sibling bug in the same function: `extra_css`/`extra_javascript` were read off the raw top-level parsed TOML dict instead of `[project]` (live-verified against `zensical.org/docs/customization/`, which nests both directly under `[project]`, not top-level) — meaning a Zensical project's real, in-use `extra_css`/`extra_javascript` files were not recognized as engine-declared, and could trigger a false-positive `Z405 UNUSED_ASSET` despite being genuinely referenced. `docs_dir`, `nav`, and `use_directory_urls` were checked against the same live sources and confirmed already correct at their existing `[project]`-level location — no further sibling gaps found. Fixed both `check_config_assets()` and `_extract_config_declared_assets()` to read from the real nested locations. TDD-first: 5 new tests in `tests/test_integrity_zensical.py` (`TestCheckConfigAssets`, `TestExtractConfigDeclaredAssets`), all confirmed red against the pre-fix code (`0 == 1` on the missing-asset detection tests; empty-set assertions on the declared-asset recognition tests) before the fix, green after. Full suite (2132 passed), `ruff`/`mypy` clean, `zenzic check all`/`mkdocs build --strict` clean.
- **Global MkDocs/Material/Zensical Claim Verification Sweep — 7 Confirmed Fabrications/Staleness/Precision Errors Fixed, 1 Real Code Defect Discovered and Logged (Not Fixed), ~25 Claims Confirmed Accurate**:
  - `docs/rules/Z405.md`: removed a leftover instance of the exact `[extra] social_image` fabrication already found and fixed in `configure-social-metadata.md` this session — Material for MkDocs has no such config key. Rewrote the sentence around the real gap (an unreferenced SVG design source, not a site-wide config key), cross-linked to the how-to page.
  - `docs/how-to/manage-cross-site-links.md`: replaced an unverifiable, incoherent claim ("Zensical does not resolve relative file-path links across plugin boundaries") — "plugin boundaries" does not correspond to any real Zensical concept, confirmed via `git log` to have been copy-forwarded unexamined across 4+ prior edits — with the real, general, verifiable reason (a relative link only resolves within its own build's source tree; no engine has cross-build visibility).
  - `docs/reference/engines.md`: removed a fabricated claim that Material for MkDocs ships a "bundled i18n plugin" under the same `i18n:` key as the community `mkdocs-static-i18n` package — live-verified against Material's own docs that Material's i18n support is theme-UI-string-only (`theme.language`), with no content-translation plugin of its own.
  - `docs/reference/engines.md`: updated the `mkdocs-awesome-pages`/`.pages` reference — live-verified the plugin was renamed to `mkdocs-awesome-nav` and its default config file to `.nav.yml` in v3; both old and new names/filenames now documented.
  - `docs/reference/engines.md`: corrected the blog plugin's dynamically-generated-routes list — RSS/Atom feed generation requires the separate `mkdocs-rss-plugin`, not something the `blog` plugin produces alone.
  - `docs/blog/posts/2026-06-13-why-we-dropped-docusaurus.md`: corrected "Zensical — Python-native engine" to "a Rust-core engine with a Python package interface" — live-verified against Zensical's own GitHub repo (`crates/` + `python/` directories, `Cargo.toml` + `pyproject.toml`), directly contradicting the prior claim.
  - `docs/blog/posts/2026-06-09-auditing-the-auditors.md`: removed an unsupported "mature" characterization of Zensical (a ~7-month-old, pre-1.0 project at the post's 2026-06-09 publish date) — "actively maintained" retained as the accurate part of the claim.
  - **Real code defect found, not fixed**: `docs/developers/explanation/adr-vault/records/adr-agnostic-universalism.md` claimed Zensical's config-asset keys are `[project].favicon`/`[project].logo` — live-verified against Zensical's own docs (`zensical.org/docs/setup/logo-and-icons/`) that the real schema nests them under `[project.theme]`. Cross-checked against `src/zenzic/core/adapters/_zensical.py:100-153`: the real, shipped `ZensicalAdapter` genuinely reads `favicon`/`logo` from the top-level `[project]` table, not `[project.theme]` — meaning `Z404` (`CONFIG_ASSET_MISSING`) cannot currently catch a missing favicon/logo on any Zensical project that follows Zensical's own documented convention. Doc corrected to state the real Zensical schema, with a dated errata note; the code defect itself logged in `03-priority-table.md` for a dedicated TDD fix, not silently patched mid-sweep.
  - **Confirmed accurate, no fix needed** (live-verified or execution-verified, not just re-asserted): `Z406.md`'s `[project.extra].alternate` Zensical schema claim (matches Zensical's own docs exactly, including the `name`/`link`/`lang` shape); the `2026-06-20-tailwind-mkdocs-material-bridge.md` post's 4 specific Material CSS/DOM claims (font-size 125% on `<html>`, no `.dark` class — uses `data-md-color-scheme` instead, header/sidebar/TOC outside the content wrapper) — all checked directly against this project's own compiled Material CSS and built-site HTML output; `compatibility.md`'s MkDocs 1.6.1/Material 9.7.7/Zensical 0.0.57 version numbers (checked against `uv.lock`, `pyproject.toml` floors, and live PyPI); the MkDocs 2.0 backward-incompatibility claims (already execution-verified repeatedly this session via the real `mkdocs build --strict` warning banner); MkDocs's `!ENV` YAML tag, `mkdocs-macros-plugin`'s Jinja2-in-Markdown mechanism, `mkdocs-static-i18n`'s `docs_structure`/`fallback_to_default` options, and Material's default `blog_dir` value — all live-verified against each project's own current documentation.
  - `mkdocs build --strict` and `zenzic check all` (98/100, 0 new) clean after all fixes.
- **`docs/reference/configuration-reference.md`'s Policy-as-Code Settings Section Was Missing 9 of 16 `[policies]` Fields**:
  - `forbidden_content_patterns` (`Z617`), `required_heading_patterns` (`Z618`), `max_document_complexity` (`Z619`), `weasel_words` (`Z519`), `enable_passive_voice_check` (`Z518`), `required_table_columns` (`Z521`), `table_cell_enums` (`Z522`), `required_heading_order` (`Z523`), and `traceability_targets` (`Z412`) are all real, live `PoliciesConfig` fields (`src/zenzic/models/config.py:308-388`) with zero documentation on this canonical reference page. Added a subsection per field, matching the page's existing style (Type/Default/Section/Finding/Opt-in table, prose, TOML example), verified against each field's real default, severity, and construction site. `traceability_targets` additionally notes that `Z412` is one of the graph-level findings that cannot be suppressed with an inline comment (`NON_INLINE_SUPPRESSIBLE_CODES`) — it requires `[governance] directory_policies`. Also corrected the section's own intro line, which named only `Z610`/`Z611` despite the (pre-fix) section already covering 7 codes. `mkdocs build --strict` clean.
- **`docs/how-to/migrate-engines.md`'s "Language Switcher Configuration" Section Reproduced `mkdocs-static-i18n`'s Own Plugin Manual at Length, the Same External-Platform-Duplication Pattern Already Fixed in `configure-social-metadata.md`**:
  - Found during a full sweep of `docs/how-to/` and `docs/reference/` for the same pattern. The section's "Recommended configuration" and "Do not" blocks reproduced two full `mkdocs.yml` YAML examples teaching `mkdocs-static-i18n`'s own `reconfigure_material`/`extra.alternate` switcher mechanics — Zenzic has no involvement in that mechanism at all. Replaced with a link to the plugin's own [Setting up mkdocs-material](https://ultrabug.github.io/mkdocs-static-i18n/setup/setting-up-material/) documentation, keeping only the genuine Zenzic-specific fact: when `reconfigure_material: true` is set, Zenzic's Virtual Site Map marks the plugin's auto-generated locale routes as reachable so they are never reported as orphans. Also removed an unfilled boilerplate scaffold sentence ("This section details the specifications and guidelines for... within the Zenzic ecosystem") and added a one-line note that this section applies only to the MkDocs engine, since Zensical uses native i18n rather than this plugin (`docs/reference/engines.md`'s existing "Why Zensical eliminates i18n complexity" section). `zenzic check all` (100/100) and `mkdocs build --strict` clean after the fix.
- **`docs/how-to/configure-social-metadata.md` Was Silent on Zensical, the Repo's Second Supported Build Engine**:
  - Live-verified against Zensical's own current documentation (`zensical.org/compatibility/plugins/`), per Tier-0 Invariant #13: a `social` module (the Zensical equivalent of Material's card-image/OG-meta mechanism) is tracked only as a Tier 2 backlog item ([`zensical/backlog#37`](https://github.com/zensical/backlog/issues/37)), not yet implemented. Added an explicit sentence and a `See Also` link stating this, rather than leaving the page silent on the engine or assuming feature parity with Material by analogy.
- **Exhaustive Cross-Repo "Docusaurus" Sweep — 3 Genuine Stale Instances Found and Fixed, Including a Currently-Live Factual Error in a Recent Blog Post**:
  - Every prior "docusaurus" fix this session was a byproduct of an audit aimed at something else; this was the first true exhaustive `grep -ri docusaurus` across all file types in all four ecosystem repositories (`zenzic`, `zenzic-action`, `zenzic-vscode`, `zenzic-mcp`), classifying every hit rather than fixing ad hoc. Full hit inventory and classification logged in `03-priority-table.md`. Confirmed clean: `zenzic-vscode` and `zenzic-mcp` (zero hits); all archived `changelogs/*.md` entries (historical records, correctly past-tense); every ADR record already correctly historically framed; `docs/_redirects`' Docusaurus-era URL migrations; `docs/explanation/architecture.md`'s `@site/` alias reference; `src/zenzic/core/{discovery,validator,resolver,rules}.py`'s docstrings/comments (generic, engine-agnostic logic using Docusaurus as an illustrative naming-convention example, not a support claim); 8 `tests/*.py` files using Docusaurus-shaped filenames/config as realistic-but-generic test fixtures for engine-agnostic logic — all 189 of their tests re-run live and confirmed passing.
  - **Genuinely wrong, fixed**: `docs/blog/posts/2026-08-11-markdown-is-production-code-why-we-built-zenzic.md` — published 2026-08-11, nearly 2 months *after* the v0.14.0 (2026-06-21) Docusaurus removal, still claimed present-tense "We support ... Docusaurus-oriented setups" and listed Docusaurus in a build-tool comparison table. This was not a historical snapshot — it was current when published and simply wrong. Corrected both to name the real current adapters, cross-linked to the removal post.
  - **Genuinely accurate when published, now stale — given the historical-snapshot treatment**: `docs/blog/posts/2026-05-27-terminal-ux-documentation-governance.md` (published *before* the June 21 removal) listed `docusaurus` as a real supported adapter mode — accurate at the time. Added a dated "Historical snapshot" note (matching the established pattern) rather than rewriting the historical record.
  - **Dead code, removed**: `src/zenzic/core/adapters/_utils.py`'s `extract_frontmatter_slug()` docstring claimed the function "works identically for MkDocs, Docusaurus, Zensical, and Standalone" — corrected to name the real 3 engines. `tests/test_standalone_mode.py`'s `_docusaurus()` helper had zero call sites anywhere in the file (confirmed via `grep -c`) — genuinely dead code, removed. `scripts/benchmark.py`'s module docstring and `--repo` help text referenced `examples/docusaurus-v3`, which does not exist anywhere in the repo — corrected to a real, live-verified example (`--repo .`, confirmed to actually run and scan 257 real files).
  - `zenzic-action`: found and fixed one instance outside this repo — `.github/dependabot.yml` had a comment claiming a nonexistent `npm-audit.yml` workflow "handles Node/Docusaurus dependency auditing separately," directly contradicting its own preceding line ("this repo has no npm packages to track"). Removed.
  - **Confirmed**: this is genuinely the last instance, closed by an exhaustive search rather than another targeted grep — no further follow-up needed for this specific recurring pattern.

- **`zenzic check all` Double-Emitted `Z201` (`CREDENTIAL_SECRET`) and `Z204` (`FORBIDDEN_TERM`) Findings — Third and Fourth Confirmed Instances of the Skip-List Bug Family**:
  - Same root cause as the already-fixed `Z202`/`Z203`/`Z108` double-emissions: a `Z201`/`Z204` `RuleFinding` is injected into `report.rule_findings` (`scanner.py`'s `_scan_single_file`) *and* `report.security_findings` is independently converted via `_map_credential_to_finding()` in the same `_to_findings()` pass — without a skip-list entry, the `rule_findings` copy surfaced a second time. Worse than a cosmetic duplicate: the `rule_findings` copy used bare `code_severity("Z201"/"Z204")` (`"error"`) instead of the CLI-layer `"security_breach"` reclassification `_map_credential_to_finding()` applies, so it rendered as an ordinary bracketed `[Z201]`/`[Z204]` list entry rather than through the `SECURITY BREACH DETECTED`/`POLICY VIOLATION DETECTED` panel — and inflated the DQS Security Override's non-suppressible-finding count (e.g. "2 non-suppressible findings detected" for one real leaked credential). Added `"Z201"` and `"Z204"` to `_check.py`'s rule-finding skip-list, mirroring the existing `Z202`/`Z203`/`Z108` entries exactly. A full sweep of every `report.rule_findings`/`report.security_findings` construction path in `scanner.py` confirmed these were the last two members of this bug family — the skip-list now exactly matches `validator.py`'s `link_codes` set (minus `Z620`, confirmed structurally exempt) plus the two security-findings-derived codes.
- **`_check.py`'s Rule-Finding Skip-List Is Now Derived, Not a Fourth Hand-Maintained Copy — Structurally Closes the Whole Bug Family Above**:
  - The skip-list above was a manually-maintained 18-member tuple literal — the same fact (which codes already surface via a dedicated path) re-declared by hand every time `validator.py`'s `link_codes` or the credential-scanner's dual-construction code set changed, which is exactly how all four confirmed double-emission bugs happened, one at a time, across separate directives this session. Promoted `validator.py`'s local `link_codes` to a module-level `LINK_CODES` constant; added a new `scanner.SECURITY_FINDING_CODES = frozenset({"Z201", "Z204"})` constant next to the credential-mapping function it describes. `_check.py`'s skip-list is now `(LINK_CODES - {"Z620"}) | SECURITY_FINDING_CODES`, computed once at module load — the literal tuple is gone. New structural test (`tests/test_check_skip_list_ssot_structural.py`, mirroring the 5 existing Severity-SSoT structural tests) parses `_check.py`'s AST and fails if any hardcoded 3-or-more-Z-code skip-list literal is ever reintroduced; a second test asserts the live skip-list equals the SSoT-derived formula. TDD evidence: red state reproduced the exact 18-Z-code hardcoded-literal violation the AST scan is designed to catch; green state after the refactor, plus a live end-to-end fixture (one `Z108` + one credential) confirming both still fire exactly once, not double-emitted, after the change. No behavior change — same 18 codes skipped as before, just derived instead of copied.
- **`docs/reference/finding-codes.md` — 4 Heading Names Drifted From `codes.py`'s `CODE_NAMES` Registry (`Z120`/`Z121`/`Z122`/`Z124`)**:
  - Implementing a new structural test (see the SSoT skip-list entry above's sibling directive item) found real, previously-undetected drift while writing it: `Z120` read `UNKNOWN_HTML_ATTR` (real: `UNKNOWN_HTML_ATTRIBUTE`), `Z121` read `MISSING_HREF` (real: `MISSING_OR_EMPTY_HREF`), `Z122` read `JUMP_LINK` (real: `JUMP_LINK_DETECTED`), `Z124` read `OPAQUE_CONTEXT` (real: `OPAQUE_HTML_CONTEXT`). The existing Mirror Law guard for this page (`tests/test_finding_codes_reference.py`) checked each heading's Severity/Penalty/Suppressible fields against `codes.py` but never the heading's own name text — exactly the shape of drift that caused a historical `Z112`-mislabeled-as-`Z108` incident earlier this session. Corrected all 4 headings; confirmed via the individual `docs/rules/Z120.md`-`Z124.md` rule cards that they already had the correct names, so the drift was isolated to this one aggregate reference page. New `test_finding_codes_heading_names_match_registry` test added to the same file, TDD evidence: red state was the real, live drift (not simulated) — the test failed with all 4 real mismatches before any doc fix was applied; green state after. Documentation only — no code behavior changed.
- **`docs/reference/advanced-features.md` Taught the Wrong Parallel-Mode Activation Threshold ("50 Files" Instead of the Real 1000)**:
  - The Hybrid Adaptive Engine table and its surrounding prose stated the parallel-scan threshold as "50 files" throughout — 20x below the real `ADAPTIVE_PARALLEL_THRESHOLD` value (`1000`). Corrected the table's three threshold rows and the explanatory paragraph below it, plus one code-comment example. One adjacent, superficially similar "50 files" mention — an illustrative example about external-URL-link deduplication, unrelated to the parallel-mode threshold — was deliberately left unchanged; it was never wrong. `docs/developers/explanation/adr-vault/records/adr-021-parallel-audit.md`'s two "50 files" mentions were left as-is with a new "Historical snapshot" note added instead: the ADR is dated 2026-05-10, when the threshold genuinely was `50` — it was raised to `1000` in v0.30.0 (2026-08-18), after the ADR was written, so the figure was accurate when authored and is a legitimate historical record, not a live defect.
- **`zenzic inspect codes` Showed `0.0` Instead of `FATAL` for `Z110`/`Z111` (`CONFIG_SYNTAX_ERROR`/`CONFIG_SCHEMA_ERROR`)**:
  - Both share `Z901`'s `error`-severity + `0.0`-penalty shape but are genuinely fatal config-abort codes (same `FROZEN_CODES` class as `Z000`/`Z001`), not HALT-style governance gates — the FATAL-detection branch only recognized a bare `Z0`/`Z2` code prefix and missed them, since they're numbered in the "Z1xx" range for historical reasons. Now renders `FATAL`, matching `Z000`/`Z001`'s existing display. In practice these two codes are LSP-diagnostic-only (their sole construction site is reachable only from the editor-integration diagnostics flow — every `zenzic check *` CLI command pre-loads its config and never triggers it), so `zenzic inspect codes` — a general code reference, not scoped to CLI-reachability — is the only place this display previously mattered.
- **`_check.py`'s `DQS Final Score` Line Showed "(Gate Failed)" After `--update-baseline`, Contradicting the Correctly-Passing Verdict Right Below It**:
  - Same root cause and same fix shape as the already-fixed `reporter.py` verdict-text bug from an earlier directive this session: the `(Gate Failed)`/`(Gate Passed)` label was computed from raw finding counts with no reference to baseline status, so a fresh `--update-baseline` run (everything baselined, real exit code 0) could show `(Gate Failed)` on the DQS line while the line directly below it correctly said "Analysis complete." Errors and strict-promoted warnings are now baseline-filtered for this label (matching the real exit-code decision); non-suppressible security findings are unaffected — a baseline can never exempt those.
- **`zenzic inspect codes` Showed `0.0` Instead of `HALT` for `Z901` (`RULE_ENGINE_ERROR`)**:
  - `Z901` unconditionally blocks the pipeline (0.0 DQS penalty, but always fails via the normal `error`-severity path) — the same practical outcome as a `warning`+0.0 HALT code (`Z504`, `Z902`), just reached a different way. The penalty-column rendering only recognized `warning`+0.0 as HALT, so `Z901` fell into the same dim `0.0` display used for genuinely harmless `note`+0.0 codes, understating that it always fails the build. Now renders `HALT`, matching `docs/reference/finding-codes.md`'s own HALT-examples table, which already listed `Z901` as an example.
- **`Z902` (`RULE_TIMEOUT`) Documentation Described an Impossible Cause**:
  - `docs/reference/finding-codes.md` and `docs/rules/Z902.md` both framed `Z902` as "a rule exceeded the execution time limit... almost always caused by catastrophic backtracking in a custom regex." Live-verified via a real pathological-rule reproduction (a genuinely slow plugin rule run through the actual parallel worker pool) that this is wrong on two counts: the real (and only live) trigger is a **file-level** worker-process stall in parallel scan mode, not a per-rule timeout — the per-rule `ZenzicRuleTimeout` exception path has zero raise sites anywhere in the engine, dead code since introduction; and `[[custom_rules]]` regex patterns cannot cause a stall at all, since they compile with Google RE2 (ZRT-007), a DFA engine with no backtracking. Both pages rewritten to describe the real mechanism (I/O hangs, network stalls, worker crashes, or a genuinely slow Python plugin rule) and the real remediation steps. Documentation only — no behavior change.
- **`find_repo_root()`'s Error Message and Docstring Undercounted Its Own Root Markers**:
  - The function's docstring claimed only `.git/` and `.zenzic.toml` are root markers, with `mkdocs.yml` "intentionally excluded" — but the function body has always also checked `zensical.toml` and `mkdocs.yml`. The raised `RuntimeError` (shown to users when Zenzic can't locate a repo root) repeated the same undercount ("no .git directory or .zenzic.toml found"). Both corrected to name all four real markers, matching `docs/explanation/configuration-loading.md`'s already-accurate description (fixed in an earlier directive). `scorer.py`'s 3 stale category-weight comments (`structural`/`navigation`/`brand`, enumerating specific Z-codes that had drifted out of date) bundled into the same cleanup pass, switched to the same SSoT-reference comment style already used for the `content` weight — internal only, no behavior change.
- **`zenzic check all --update-baseline`'s Verdict Text Contradicted the Real Exit Code**:
  - When every current finding is baselined (e.g. immediately after `--update-baseline` creates a fresh baseline from the current run), the real exit code is 0 — but the printed verdict still read `"FAILED: Hard errors detected. Exit code 1 is mandatory."`, contradicting the process's actual outcome in the same breath. Root cause: `reporter.py`'s verdict-message selection (`has_hard_failures`) was computed from raw finding counts with no reference to each finding's `is_baselined` flag, while the real exit-code decision (`_check.py`) is baseline-aware. `ZenzicReporter.render()` gained a `baseline_active` parameter; when set, the verdict now excludes baselined errors/strict-promoted-warnings from the FAILED/OK decision, matching the real exit code. Non-suppressible security findings (breaches, incidents) are unaffected — a baseline can never exempt those. The printed summary line's raw counts (`✘ N errors`) and the function's returned `(error_count, warning_count)` tuple are both unchanged — only the FAILED/OK verdict text changed. `check all` without `--baseline`/`--update-baseline` is unaffected (live-reverified: still shows the FAILED text as before).
- **`zenzic check all` Double-Emitted `Z108` (`EMPTY_LINK_TEXT`) Findings**:
  - Same root cause and same fix shape as the already-fixed `Z202`/`Z203` double-emission: `Z108` is a member of `validator.py`'s `link_codes` set, so its `RuleFinding` already surfaced once via `validate_links_structured()`'s `LinkError` path — without a matching entry in `_check.py`'s rule-finding skip-list, it surfaced a second time via the generic rule-finding loop. Added `"Z108"` to the skip-list. Confirmed no other `link_codes` member has the same gap: `Z620` was previously checked and found not to share this bug (its findings route through a structurally separate code path). A project with a single empty-link-text finding previously saw it counted (and penalized) twice — confirmed via a live before/after run on the `examples/z108-empty-link-text/` gallery fixture: DQS score 98 → 99, `structural` category `issues` 2 → 1, `raw_penalty` 2.0 → 1.0 pts. It now counts once, matching every other `link_codes` member's existing behavior.
- **Three CLI Flag-Conflict Errors Wrongly Exited 2 (Security Code), Now Exit 1**:
  - `zenzic check all --strict --exit-zero` and `zenzic init --plugin <name> --local`/`--pyproject` raised `Exit 2`, the code the Tier-0 Exit Code Contract reserves exclusively for security breaches (`Z201`/`Z204`/`Z205`). All three are plain CLI-usage errors (an invalid flag combination caught before any analysis runs), not security findings. Confirmed by git history to be inconsistent, not intentional: `init`'s own `--local`+`--pyproject` conflict (added 3 weeks after `--plugin`'s conflict check, same command) already used `Exit 1` for a structurally identical error. All three sites now raise `Exit 1`; a new regression test locks all three of `init`'s conflict pairs to the same exit code. `zenzic diff`'s fatal-detection logic also dropped an unreachable `Z0xx` branch (config-abort codes like `Z001` always raise `ConfigurationError`, and exit, before this code path can run — confirmed dead, not just simplified defensively) — behavior unchanged, `Z2xx` security findings still trigger `Exit 2` here as before. If any script currently branches on `Exit 2` from `check all --strict --exit-zero` or `init --plugin ...`, it will now see `Exit 1`.
- **2 Blog Posts Had the Z202/Z203 Exit-3 Conflation Not Time-Scoped as Historical**:
  - `2026-06-27-v016-magnetite.md` presented an exit-code table as "a stable, platform-agnostic exit-code contract" (not framed as a version-specific snapshot) grouping `Z202`/`Z203` under Exit 3 — corrected to name `Z203` only. `2026-05-25-dqs-mathematical-model.md`'s Security Override formula stated the set as `{Z201, Z202, Z203, Z204}` without disclosing this was the v0.8.0-era set; `Z205` was added in v0.17.0, after this post was written — added a "Historical snapshot" note matching the same post's existing precedent for its Z103/Z111/Z113 table. Documentation only.
- **`cli.md`'s "How the Score Is Computed" Section Described an Obsolete Scoring Model from Before v0.6.x**:
  - Replaced the stale `links/orphans/snippets/placeholders/assets` decay-based description (which directly contradicted the current `structural/navigation/content/brand` bucket-cap model — and contradicted the page's own `--breakdown` example directly below it) with an accurate description of the real model, cross-linking `scoring-algorithm.md` for the full formulas. The adjacent "DQS MATHEMATICAL TRANSPARENCY" example block, whose labels and a fabricated "Category Subtotal" line matched no real CLI output at all, was replaced with a live-captured real example. Documentation only — no code behavior changed.
- **`docs/reference/finding-codes.md`: 8 Diagnostic-Code Entries (Z410-Z412, Z510-Z523, Z617-Z619) Had No Category Header — Structurally Orphaned, Not Reachable Under Their Correct `## Z4xx`/`Z5xx`/`Z6xx` Sections**:
  - Relocated all 8 entries into their correct category sections in ascending numeric order; the real `## Z4xx — Structure`, `## Z5xx — Content Quality`, and `## Z6xx — Governance` sections were each missing exactly these codes. Documentation only.
- **`zenzic-action-wrapper.sh`, `docs/architecture/lsp-integration.md`, and the VS Code extension's Z201 notification all pointed to a `zenzic.dev/docs/`-prefixed URL that 404s**:
  - Corrected to the real `zenzic.dev/reference/finding-codes/` URL (same defect class as an already-fixed `README.md` issue). No code behavior changed — reference-link text only.
- **`docs/reference/configuration-reference.md`: ~15 Field/Behavior Defects Corrected, Most Severe Being a Self-Contradicting Claim That a Fatal Config Error Is Silently Discarded**:
  - The "Field Order is Law" section claimed a misplaced root-level key inside an unknown TOML section is silently discarded — the real behavior (confirmed against `ZenzicConfig._validate_no_swallowed_root_keys()`) is a **fatal** `ZenzicConfigError` crash for exactly the example shown, directly contradicting the very next subsection on the same page. Also corrected: the System Guardrails list (2 phantom entries removed, 9 missing real ones added), the engine auto-discovery priority order (was missing the `.zenzic-vsm.json`/`prebuilt` and zensical-theme-detection steps), the `engine` field's documented type, 3 undocumented config fields added (`max_sentence_length`, `absolute_path_allowlist`, `offline_mode`), a case-sensitivity claim for `brand_obsolescence` (doc said case-sensitive, engine is case-insensitive), and a `zenzic config explain` Track-2 blind spot disclosure. Documentation only.
- **6 More `docs/reference/` Pages Corrected — Nonexistent Class/Path/Directory Names, Stale Code Counts, Missing Track-2 Examples**:
  - `brand-system.md`: nonexistent `src/css/custom.css` path corrected to the real `docs/assets/css/extra.css`; badge colors decoupled from `ZenzicPalette` (an unrelated CLI terminal-color class); badge SVG canonical-location claim corrected to distinguish the docs-built asset directory from a separate, non-build one.
  - `brand-kit.md`: stale `zinc` color-family claim corrected to the real `slate` tokens, with WCAG contrast ratios recomputed against the actual current values.
  - `index.md`: stale "36+ diagnostic codes" claim corrected to the real, live-verified count (72); added 2 missing rows to its own reference-page index table; added a Track-2 (`pyproject.toml`) cross-link.
  - `glossary.md`: nonexistent `ShieldViolation` exception class corrected to the real `CredentialViolation`; a false claim that `__pycache__` is an unconditionally-excluded system directory removed; a stale heading anchor fixed.
  - `suppression-policy.md`: added a Track-2 cross-link for its `[governance]` example.
  - `scoring-algorithm.md`: its Navigation code-range summary was missing `Z401`, inconsistent with the page's own penalty table further down — added. Every scoring formula on this page (weight matrix, per-code penalties, Governance Escalation, Gravity Cap, Suppression Debt) was independently re-verified against real `zenzic score --breakdown` output from live fixtures, not just source comparison — all confirmed accurate, no other changes needed.
  - No code behavior changed — documentation only.
- **`docs/reference/zenzic-action.md` and `docs/reference/api-json.md` Described Non-Existent Inputs and Wrong Output/Formula Semantics**:
  - `zenzic-action.md`'s Inputs table listed two GitHub Action inputs that do not exist (`ci`, `only`) and omitted two real ones (`working-directory`, `generate_audit_report`); `--ci` is unconditionally hardcoded in the wrapper, not input-driven — corrected. The `score` output's "populated only for `format: json`/`diff-base`" claim was wrong (the DQS Governance Gate runs regardless of `format`); `cap-exceeded` is SARIF-format-only (hardcoded `"false"` otherwise) and was undocumented — both corrected.
  - `api-json.md`'s `suppression_debt_pts` field was documented with a single formula that is only correct for the CAP Fail-Hard JSON shape; the `check all`/`score` shapes actually use a flat per-suppression count with no cap subtraction (confirmed against two independent source files computing the field differently) — now documented per-shape. The CAP Fail-Hard example's `remediation` array was missing 2 of its 3 real entries (corrected against live-captured output). The SARIF `helpUri` example had a phantom `/docs/` URL prefix and a missing trailing slash; `properties.category`'s documented value list was missing the real `"uncategorized"` fallback — both corrected.
  - No code behavior changed — documentation only, all corrections verified against real source and/or live command output.
- **6 `docs/reference/` Pages Misattributed Exit 3 to Z202 — Same Conflation Pattern, Docs Only**:
  - `glossary.md`, `suppression-policy.md` (table row + callout), `cli.md` (Arsenal Inspection section, stale copy of the now-fixed `_inspect.py` docstring), `zenzic-action.md` (Exit Code Contract table), and `api-json.md` (`security_breaches`/`security_incidents` field description, ambiguous `Z2xx` shorthand) all stated or implied that `Z202` shares `Z203`'s Exit 3. `Z202` has always deliberately stayed at plain Exit 1 (unchanged, already test-locked). All 6 corrected to distinguish `Z202` (Exit 1, non-suppressible but not escalated) from `Z203` (fatal, Exit 3). No code behavior changed — documentation only.
- **`zenzic inspect capabilities` Misattributed Exit 3 to Z202**:
  - The "Core Scanners" table's `CORE_SCANNERS` registry (`codes.py`) previously listed a single merged `Z202-203` row with `primary_exit=3`, implying `Z202` (ordinary docs-root-boundary traversal) shares `Z203`'s (fatal, OS-system-directory traversal) Exit 3. `Z202` has always deliberately stayed at plain Exit 1 in the actual exit-code logic (unchanged, already test-locked by `tests/test_cli.py::test_z202_still_maps_to_plain_error`) — this was a display-only defect in the introspection table, not a real exit-code bug. Split into two entries, `Z202` (Exit 1) and `Z203` (fatal, Exit 3), each independently non-suppressible; `zenzic inspect capabilities`'s docstring corrected to match. New regression test: `tests/test_codes.py::test_core_scanners_z202_z203_not_merged_under_one_exit_code`.
- **Severity SSoT Invariant Now Enforced Across All Seven Finding-Construction Subsystems**:
  - `governance.py`'s `PolicyEvaluator` (10 sites, `Z610`-`Z619`), `suppressions.py` (4 sites, `Z603`/`Z620`), and `content.py` (22 sites, `Z510`-`Z523`) were three previously-unguarded subsystems that constructed findings with bare hardcoded `severity=` string literals instead of deriving them from `codes.py`'s `CODE_DEFINITIONS` via `code_severity()` — the same architectural gap already closed this release for `_check.py`, `rules.py`, `incremental.py`, and `scanner.py`. All 36 sites currently matched `codes.py` (no live user-facing bug), but were unguarded against silent divergence on the next `codes.py` edit. All three now route through `code_severity()`, with new structural AST tests (`tests/test_governance_severity_ssot_structural.py`, `tests/test_suppressions_severity_ssot_structural.py`, `tests/test_content_severity_ssot_structural.py`) proven against each file's pre-fix state (10/4/22 violations respectively, matching the exact confirmed counts). The Severity SSoT invariant now has full construction-site coverage across the engine.
- **`check links`/`check placeholders` Never Detected Credential Leaks — Security-Behavior Change**:
  - `zenzic check links` and `zenzic check placeholders` now correctly detect credential/secret leaks (`Z201`) and forbidden terms (`Z204`) and exit `2`, matching `check all`'s existing Tier-0 "Exit 2: Credential Scanner Breach — Never suppressible" contract. **Previously, both subcommands silently discarded credential-scanner results**: the underlying scan already ran on every file (as part of the same `scan_docs_references()` pipeline `check_all` uses), but neither subcommand ever read `IntegrityReport.security_findings` — a leaked AWS key or forbidden term in a file scanned via `check links`/`check placeholders` alone produced zero signal and an ordinary exit `0`/`1`. **If you run `zenzic check links` or `zenzic check placeholders` in isolation (e.g. as a narrower pre-commit hook than `check all`) and your script or CI job only expects exit codes `0`/`1` from these two commands, it may now receive exit `2` (or `3`, for a fatal path-traversal finding) where it previously would not have — this is the fix taking effect, not a new false positive.** No performance impact: the credential scan already ran; this only surfaces results that were already computed.
- **Z205 Exit-Code/Severity Contract (Tier-0)**:
  - `Z205` (`FORBIDDEN_SCHEME`) now maps to `security_breach` severity in `_finding_severity()`, restoring the Tier-0-mandated Exit 2 and non-suppressible behavior it shares with `Z201`/`Z204`. Previously fell through to its raw catalog severity (`error`, Exit 1) since it never passes through the credential-scanner bridge that sets `security_breach` for `Z201`/`Z204`.
- **`check links` Exit-Code Gap for Security Breaches**:
  - `zenzic check links` now exits 2 (and honors `security_breach` severity) in every output format (text, JSON, SARIF, GitHub annotations), matching `check all`'s existing behavior. Previously it only checked `security_incident` (Exit 3) and `error` (Exit 1) — a `Z205`-class finding fell through to Exit 1 instead of the Tier-0-mandated Exit 2.
- **`check all --format json` Missing Security-Breach Marker**:
  - The `checkAllReport` JSON payload now includes `security_breaches` and `security_incidents` integer count fields, so a JSON consumer can detect a breach without parsing link-error message text or relying solely on the process exit code. `zenzic-output.schema.json` updated to match.
- **`zenzic lab` Exit Code Now Reflects Scenario Pass/Fail**:
  - `zenzic lab <code>`/`zenzic lab all` now exit 1 if any scenario does not meet its expectation, and 0 otherwise — previously always exited 0 regardless of the printed PASS/FAIL verdict, which defeated `lab all`'s use as a regression gate (e.g. in CI).
- **`check <file>` VSM Scoping Bug for Files Outside `docs_dir`**:
  - `zenzic check all <file>` (and other single-file `check` invocations) on a file outside the configured `docs_dir` (e.g. `CHANGELOG.md`, `README.md`) now preserves the full site's VSM instead of rebuilding one scoped to just that file's parent directory. Previously this produced false `Z103`/`Z410`-style "unreachable" findings for links to the rest of the real site. Matches the Language Server's existing behavior (`_resolve_docs_root()` always resolves the full workspace, never re-scoped per file).
- **Z107 False Positive on Same-Page Cross-References (rule logic fixed)**:
  - Two same-page cross-reference links in `docs/reference/finding-codes.md` whose visible text collided with their target anchor's slug (tripping `Z107 CIRCULAR_ANCHOR`'s same-text-as-fragment heuristic even though they correctly pointed to a different section) were reworded. `Z107`'s underlying rule logic has since been fixed: `CircularAnchorRule` now tracks the containing ATX heading while scanning and only flags a same-slug anchor link when the *containing* section's own heading slug matches the link's fragment — a genuine self-reference (e.g. `[Setup](#setup)` inside `## Setup`). A link whose text merely happens to slugify to the same fragment as a *different* section elsewhere on the page (e.g. `[Z101](#z101)` written from inside a `## Z104` section) is no longer flagged. Previously any occurrence of matching link-text-to-fragment text was flagged regardless of position, including inside code spans/fenced code blocks describing the syntax (the rule has no fence-awareness) — this also silently required a `.zenzic.toml` directory-wide suppression for `docs/tutorials/examples/z1xx-links/**`, now removed as genuinely dead configuration.
- **LSP Code Action UX Determinism for Topological Findings (ADR-093)**:
  - The Language Server Protocol server (`src/zenzic/lsp/server.py`) now recognizes `NON_INLINE_SUPPRESSIBLE_CODES` (`Z401`, `Z402`, `Z404`, `Z405`, `Z406`, `Z410`, `Z411`, `Z412`, `Z620`).
  - Instead of offering an ineffective inline comment QuickFix edit, the LSP emits a standard `disabled` CodeAction explaining: *"Zxxx is a topological finding. Configure suppression in .zenzic.toml via [directory_policies] or [per_file_ignores]."*, eliminating misleading edits and protecting the TOML Root Key Law.
- **`check <file>` DQS Score No Longer Silently Mixes File and Project Scope**:
  - The suppression/technical-debt penalty in `zenzic check all <file>`'s DQS score (and the `suppression_count`/`suppression_debt_pts`/`debt_status` fields in its `--format json` payload) is now scoped to the target file when one is given, matching the Z-code finding penalties, which were already file-scoped. Previously, a single-file scan's score correctly excluded findings from other files but still applied the *whole project's* suppression debt, producing a hybrid number that was neither a true per-file score nor a true project score, undocumented either way. The suppression CAP fail-hard gate and the `🔒 Suppression Audit` text footer remain intentionally project-wide (the CAP is a project-level governance ceiling, not a per-file concept).
- **`check <file>` Text Output Now Labels the Suppression Audit Footer's Scope**:
  - The `🔒 Suppression Audit: N/cap` footer now appends `(project-wide)` when `check all <file>` is given a single-file target, since the DQS score line directly above it is file-scoped (see above) but this footer's number deliberately stays project-wide (the suppression CAP is a project-level governance ceiling, not a per-file concept). Previously the two adjacent lines showed silently different, unstated scopes. A full-project scan (no target) is unaffected — no label is added since there is no scope mismatch to disambiguate.
- **`check <file>` on a Target Outside `docs_dir` Never Ran the Rule Engine On It At All**:
  - `zenzic check all <file>` for a target outside the configured `docs_dir` (e.g. `CHANGELOG.md`, `README.md` at repo root) now correctly runs the rule engine (all `Z1xx`-`Z6xx` content/editorial rules) on that target file. Previously, no code path ever added an out-of-tree target into the scan's file set, so such a target silently received zero rule-engine findings and a falsely perfect score, with no crash or warning. Verified against the real repository's own `CHANGELOG.md`: previously 0 findings / `DQS 98/100`; now correctly reports `Z512` (a genuine empty-heading-section finding) / `DQS 99/100`.
- **`check <file>` No Longer Runs the Full Rule Engine on Every Project File**:
  - `zenzic check all <file>` now skips the rule-engine pass (AST parsing + all `Z1xx`-`Z6xx` rules) on every file except the target — previously it ran unconditionally on the entire project (e.g. all 262+ files) regardless of the single-file target, then discarded all but the target's findings after the fact. Measured ~90% of the rule-engine's own cost eliminated (2.45s → 0.03s in a profiled run). Full VSM construction and Pass 1-3 (security/link/reference) scanning still run project-wide by design — required for correct topology, credential scanning, and link resolution — so overall wall-clock time remains roughly proportional to project size, not target-file size (measured ~18-26% total reduction, not an order of magnitude). True near-instant single-file checking would require a persistent process sharing a cache across invocations, the way the Language Server already does — not achievable as a CLI-only fix; tracked as part of the open CLI/LSP shared-analysis-primitive architectural decision (see Known Limitations below).
- **`check all --format json`'s `references` Field Was Missing Rule-Engine Findings and All Warnings** (non-breaking enrichment, not a removal):
  - The `checkAllReport` JSON payload's `references` array now includes rule-engine findings (`Z1xx`-`Z6xx` content/editorial rules, e.g. `Z502` SHORT_CONTENT, `Z512` HEADING_SECTION_EMPTY) — previously these lived on a separate `IntegrityReport.rule_findings` attribute that `_output_check_all_json_findings` never read, so they were always silently absent from this field even when text/SARIF output correctly reported them. The pre-existing filter that excluded warning-severity reference findings (Z1xx/Z3xx) from this field was also removed for internal consistency: the field is named `references`, not `reference_errors`, and text/SARIF output already report both severities. Existing consumers reading `references` as an array of strings are unaffected — the schema shape (`zenzic-output.schema.json`) is unchanged; only the array's contents grow to match what other output formats already reported. `zenzic-mcp` does not consume this payload (its `check_document` tool is built against `IncrementalAnalysisEngine` directly, precisely to avoid depending on this CLI-private JSON shape) — the real consumers are external CI integrations and third-party tooling invoking `zenzic check all --format json` directly.
- **`nox -s mutation`'s Working-Copy Staging Was Missing Every Non-`src/` File the Build and Test Suite Require**:
  - `[tool.mutmut] also_copy` (`pyproject.toml`) now includes `README.md` and `examples/` (required by the hatchling build backend's `[project].readme` and `[tool.hatch.build.targets.wheel.force-include]`) alongside `zenzic-baseline.schema.json` and `zenzic-output.schema.json` (required by schema-compliance tests) — previously only `src/` was staged, so `mutmut run`'s pre-flight baseline collection failed immediately (0 of 2,338 mutants ever tested) with a missing-file error, silently making the Core's entire mutation-testing gate non-functional. Enumerated by reading the build backend's own config sections directly, not by trial and error.
- **Z110 Code-Identity Collision (BREAKING for existing baselines)**:
  - `Z110` used to be emitted by two independent, semantically unrelated code paths: `src/zenzic/models/config.py` for `CONFIG_SYNTAX_ERROR` (a non-suppressible pre-scan TOML-syntax guard) and `src/zenzic/core/scanner.py` for `STALE_ALLOWLIST_ENTRY` (a suppressible warning about an unused `absolute_path_allowlist` entry). `codes.py`'s own schema docstring — self-described as "the single source of truth for code assignments" — still documented the old `STALE_ALLOWLIST_ENTRY`/`VIRTUAL_ROUTE_BROKEN` meanings for `Z110`/`Z111` while its `CODE_DEFINITIONS`/`CODE_NAMES` dicts a few hundred lines below had already moved on to `CONFIG_SYNTAX_ERROR`/`CONFIG_SCHEMA_ERROR`, self-contradicting within the same file. Because `Z110` is in `NON_SUPPRESSIBLE_CODES`, and the DQS security override collapses the score to 0/100 for *any* finding under a non-suppressible code, a stale allowlist entry — an ordinary configuration-hygiene nit — silently zeroed a project's entire document quality score exactly like a genuine credential leak would.
  - `STALE_ALLOWLIST_ENTRY` is now emitted as **`Z112`** (the previously-reserved, unused slot), leaving `Z110` = `CONFIG_SYNTAX_ERROR` untouched. `Z111` was investigated and confirmed dead code — `VIRTUAL_ROUTE_BROKEN` had no live emission site anywhere in the engine (only in stale docs and the schema docstring); no renumbering was needed for it, only a documentation/docstring correction to `CONFIG_SCHEMA_ERROR`, its actual current live meaning.
  - **Action required**: if your saved `.zenzic-score.json` baseline (or any historical SARIF/JSON output you diff against) was generated before this release and counts `Z110` for a stale-allowlist condition, that count now belongs under `Z112` instead. Run `zenzic score --save` (or your project's equivalent baseline-update command) after upgrading to avoid a false `zenzic diff` regression report against a baseline keyed on the old code assignment.
- **`pyproject.toml`'s `[tool.coverage.report] fail_under = 80` Was Declared But Never Actually Checked by Any Automated Pipeline**:
  - `just verify`'s test step now runs `pytest` with `--cov=src/zenzic --cov-report=term-missing`, printing total coverage and the gap to 80% on every run. `test-cov`/`test-cov-full` were previously the only recipes that ever measured coverage, and neither `just verify`, pre-commit, nor any GitHub Actions workflow ever invoked them — the 80% floor had genuinely never been checked in any automated context prior to this release, not merely bypassed. Coverage is measured but does not yet fail the build in this initial wiring step (`--cov-fail-under=0`); enforcement follows in the next entry once the real gap is closed.
  - **Gap closed**: real coverage measured at 79.31% (below the declared 80% floor). Added targeted tests for four previously-uncovered or near-uncovered modules — `zenzic.cli._lsp` (0%→100%, the `zenzic lsp` command wrapper), `zenzic.core.adapters._prebuilt` (33%→100%, `PrebuiltVSMAdapter` had no tests at all), `zenzic.core.adapters._mkdocs_config` (78%→100%, the permissive MkDocs YAML tag loader's `!ENV`/`!relative`/`!!python`/unknown-tag constructors), and `zenzic.cli._config_explain` (13%→97%, the `zenzic config explain` command had zero prior coverage). No filler tests — each covers real, previously-unexercised branches (error paths, source-precedence logic, tag-constructor variants). Total coverage now 80.76%, genuinely above the floor. Enforcement flipped to hard-blocking (`--cov-fail-under` removed, relying on `pyproject.toml`'s `fail_under = 80`); verified to actually block by temporarily deleting a covering test and confirming `just verify`'s test step fails with a non-zero exit before restoring it.
  - **CI never invoked any coverage-measuring recipe at all**: `.github/workflows/ci.yml`'s "Run unit tests" step called `just test` (no `--cov`), independent of and in addition to `just verify`'s own gap above — the two paths never shared a recipe. Switched to `just test-cov` (the serial, coverage-instrumented recipe that already existed but was never wired into any automated pipeline), so CI now enforces the same 80% floor.
- **`check all` Double-Emitted `Z202`/`Z203` Path-Traversal Findings**:
  - `zenzic check all` now reports a single `Z202`/`Z203` finding per dangerous link, matching `zenzic check links`'s existing (correct) behavior. Previously the same finding was rendered twice: once via the generic rule-finding loop and again via `validate_links_structured()`'s `LinkError` path, since `Z202`/`Z203` were the only two members of `validator.py`'s `link_codes` set missing from `_check.py`'s de-duplication skip-list. This also silently doubled the DQS security-override penalty count (e.g. "2 non-suppressible findings detected" for what was actually one) for any project with a genuine path-traversal finding.
- **`check all` Printed "Exit code 1 is mandatory" on an Actual Exit-3 Run**:
  - The terminal verdict on a run with a `Z203`-class security incident now correctly reads "Security incidents detected. Exit code 3 is mandatory." Previously `reporter.py`'s verdict-message chain had no branch for `security_incident` findings and silently fell through to the generic "Hard errors detected. Exit code 1 is mandatory." text, even though the process's actual exit code was already correctly `3` — a display-only bug, the real exit code was never wrong.
- **`docs/rules/Z521.md`, `Z522.md`, `Z523.md` Documented the Wrong Suppression Mechanism**:
  - These three rule cards' Suppression sections now document the inline `<!-- zenzic:ignore:ZXXX -->` comment form (matching the sibling `Z516.md`-`Z520.md` pages) ahead of the pre-existing `.zenzic.toml` directory-level example. Previously they showed only the directory-level TOML form, even though all three codes are genuinely absent from `NON_INLINE_SUPPRESSIBLE_CODES` and inline suppression works for them — the simpler, single-line suppression option was undocumented.
- **`docs/how-to/install.md` and `initialize-configuration.md` Corrected Against Actual CLI Behavior** (first hygiene-audit pass over `docs/how-to/`):
  - `install.md`'s `zenzic init` example output was fabricated and didn't match the real Rich panel text (`src/zenzic/cli/_standalone.py:1561-1573`), and omitted that a bare `zenzic init` also scaffolds `.zenzic.local.toml` in the same run — corrected to the actual panel content. A separately quoted Helpful Hint panel also dropped "lint" from "engine adapter, and lint rules" (`_shared.py:132`) — restored.
  - `initialize-configuration.md` documented `zenzic init --force` as valid usage ("overwrite an existing file"), but `--force` is explicitly rejected for configuration initialization (`_standalone.py:1361-1366`: "ERROR: --force is not supported for configuration initialization.") — removed from the command example and replaced with a note on the actual behavior. The sample `.zenzic.toml`'s commented-out `excluded_dirs` default incorrectly included `"assets"`; the real default (`src/zenzic/models/config.py:559-560`) is `["includes", "stylesheets", "overrides"]` — corrected.
- **`docs/how-to/configure-adapter.md` and `configure-ci-cd.md` Corrected Against Actual Engine-Discovery and Exit-Code Behavior** (how-to/ Batch 2 hygiene audit):
  - `configure-adapter.md`'s "Auto-Discovery priority order" listed only 3 of the real 5 steps in `discover_engine()` (`src/zenzic/core/adapters/_factory.py:94-129`), and its own numbering skipped "2" — missing the `.zenzic-vsm.json` prebuilt-artifact check and the `mkdocs.yml`-with-`theme: zensical` compatibility check. Corrected to the real 5-step order. The unsupported claim that `[build_context]` "must be the **last** section in `.zenzic.toml`" (no such enforcement exists anywhere in the config loader) was removed. The `--engine` flag's documented scope ("`zenzic check orphans` and `zenzic check all`") was missing two real sites (`zenzic clean assets`, `zenzic init`) — corrected. Added the missing `[tool.zenzic.build_context]` `pyproject.toml` equivalent (Track-2 coverage gap).
  - `configure-ci-cd.md`'s Exit Code Contract callout presented orphan pages (`Z402`) and placeholder text (`Z501`) as unconditional Exit-1 triggers; both are `warning`-severity by default (`codes.py`) and only trigger Exit 1 under `--strict` (which the page's own `--ci` examples enable automatically, but the callout didn't state the dependency) — corrected to state the condition explicitly. The callout was also mislabeled "(ADR-075)" — ADR-075 is Radical Unawareness, not the Exit Code Contract, which has no ADR citation in `01-tier-0-invariants.md` — the incorrect ADR reference was removed.
  - `configure-ci-cd.md` documented `zenzic guard init` as scaffolding/updating the `.pre-commit-config.yaml` shown in the same section; it actually creates/updates a different file, `.pre-commit-hooks.yaml` (`_guard.py`'s `init_guard`, `--path` default) — clarified. Three `zenzic check all --format {sarif,json} --save <file>` examples used a `--save <filename>` flag that does not exist on `check` (grepped `_check.py` in full) — `--save` exists only on `zenzic score`, as a boolean with no filename argument, always writing the fixed `.zenzic-score.json` path. Corrected to shell redirection (`> file`), which is how these formats are actually emitted (both print to stdout). The GitLab CI example additionally declared the resulting JSON under `artifacts.reports.codequality`, but Zenzic's `--format json` output is a category-keyed object, not GitLab's required array-of-objects Code Quality schema — changed to a plain artifact upload with a note explaining why.
  - `configure-privacy-gate.md` claimed `[governance] forbidden_patterns` (`Z204`) supports "RE2 DFA syntax... for patterns that require regex matching" — this field is literal-string-only (`credentials.py:361-363`; every pattern is `re.escape()`-d before compilation, `config.py:838`); the RE2 union pass mentioned in source is an internal O(1) matching optimization, not a user-facing regex feature — corrected. A hedged, non-existent `--forbidden` CLI flag ("if available in your Zenzic version") was removed — grepped the full CLI, no such flag has ever existed. The Precedence section described `.zenzic.toml` and `pyproject.toml [tool.zenzic]` as an "override" chain; they are mutually exclusive fallback (`.zenzic.toml` used if present, `pyproject.toml` only read as fallback when absent, `config.py:1324-1333`), never merged — corrected, trimmed to defer to `configuration-reference.md`'s existing precedence section rather than restating it.
  - `configure-social-metadata.md` claimed a social-card image referenced only via frontmatter (`image: ...`) or `[extra]` config counts as "used" and avoids Zenzic's `Z405` unused-asset flag. Zenzic's asset-reference scanner (`scanner.py`'s `check_asset_references`) only recognizes real Markdown/HTML links — frontmatter and config keys are not parsed as references — so an image referenced only as shown in the page's own examples would in fact be flagged. Corrected, and the `excluded_assets` example broadened accordingly; added the missing `pyproject.toml [tool.zenzic]` Track-2 equivalent.
- **`Z405` (`UNUSED_ASSET`) False Positive on Frontmatter-Only Asset References**:
  - `check_asset_references()` now recognizes a page's own `image:` frontmatter key (the social/OG-card convention documented in `docs/how-to/configure-social-metadata.md`) as a genuine asset reference, alongside the existing Markdown-link, HTML-tag, and AST reference-definition recognition. Previously, an asset referenced *only* via frontmatter was incorrectly flagged as unused, requiring a manual `excluded_assets` entry for every such image. Site-wide engine-config references (`[extra] social_image` in `mkdocs.yml`/`zensical.toml`) are still not recognized and still need explicit exclusion.
- **`Z620` (`STALE_GLOBAL_SUPPRESSION`) Never Detected a Dead `per_file_ignores` Entry**:
  - `GlobalUsageTracker` now tracks `[governance.per_file_ignores]` usage as a fourth source, alongside `directory_policies`, `excluded_file_patterns`, and `excluded_external_urls` — a `per_file_ignores` entry that never actually suppressed a finding (e.g. a stale code or a file pattern nothing matches) is now correctly flagged `Z620`, matching the detection already in place for the other three governance-exemption mechanisms.
- **`docs/how-to/add-badges.md`, `add-custom-rules.md`, and `handle-technical-debt.md` Corrected Against Actual CLI/Schema Behavior** (how-to/ Batch 3 hygiene audit):
  - `add-custom-rules.md` repeated the same unsupported "`[build_context]` must be the last section" claim already corrected once this session in `configure-adapter.md` — removed, replaced with the real constraint (root-level keys must precede any table header). Claimed custom-rule patterns are matched with Python `re.search`; they are compiled with RE2 (`zenzic.core.regex`, ADR-013), which does not support lookaheads/lookbehinds/backreferences — corrected, with the restriction now stated explicitly. The "Need structural analysis?" section pointed readers to the removed v2 API (`BaseASTRule`, hard-deprecated in v0.28.0) instead of the current Custom Rule SDK v3 (`ZenzicRuleV3`) — rewritten.
  - `add-badges.md`: added the missing `[tool.zenzic.project_metadata]` `pyproject.toml` Track-2 equivalent for `badge_stamp_files`.
  - `handle-technical-debt.md` claimed all Z2xx security findings' "exit code is still 2 or 3"; `Z202` is deliberately not escalated and remains Exit 1 (`codes.py`, `tests/test_cli.py::test_z202_still_maps_to_plain_error`) — corrected, and the surrounding section trimmed to point at `docs/reference/suppression-policy.md`'s canonical code/exit-code table instead of restating it. Added the missing `[tool.zenzic.governance]` Track-2 equivalent for `suppression_cap`/`suppression_cap_fail_hard`.
- **`docs/how-to/` Batch 4 — Final 5 Pages Corrected, Completing the First Full Pass Over the `how-to/` Quadrant**:
  - `manage-cross-site-links.md` claimed the live, functioning `absolute_path_allowlist` config field ("unsupported... raises a TOML validation error at startup") had been removed, steering readers toward a worse per-line workaround instead of the real, documented fix — corrected with a working example and its `pyproject.toml` Track-2 equivalent. Also fixed a table cell showing identical HTML-comment syntax for both the Markdown and MDX suppression-comment forms.
  - `migrate-engines.md`: removed a fabricated CLI banner (`"NOTICE: Zensical engine active via mkdocs.yml compatibility bridge."`, no such string exists in source) and invented "Transparent Proxy"/"Transparent Bridge" terminology; corrected a claim that declaring `engine = "zensical"` requires `zensical.toml` to be present, which directly contradicted the page's own later section describing the real `mkdocs.yml` fallback; corrected the frontmatter description, which described version-upgrade notes for a page actually about switching build engines.
  - `troubleshooting.md`: corrected a claim that `--strict` gates external link validation (it doesn't; `--no-external` disables it, and validation runs by default); corrected a claim that any Z2xx Security Override "always exits 2 regardless of `fail_under`" (`Z202` deliberately stays Exit 1); added the missing `Z205` to the non-suppressible Z2xx code list; replaced two instances of a fabricated `zenzic config show` command with the real `zenzic config explain`; removed a fabricated `--verbose` flag on `check all`; corrected a stale minimum Core version claim (`v0.23.1` → the real `v0.30.0`, `zenzic-vscode`'s `MIN_CORE_VERSION`).
  - `workflow-integration.md`: removed a fabricated `--no-fail-under` flag (no such flag exists; `--exit-zero` is the real always-exit-0 mechanism); fixed a `just` recipe with inconsistent indentation and a `Makefile` recipe using spaces instead of the tabs Make requires (both would fail to run as shown); removed a redundant, self-contradicting "without uv" recipe variant; corrected a fabricated terminal-output block (wrong code name `[EXTERNAL_LINK]` → the real `[Z109]`); wired the documented `ZENZIC_EXTRA_ARGS` environment variable into the example recipes it claims to affect (previously shown unused).
  - `editor-integrations.md`: removed "Zed" from the title — the page has zero Zed-specific content in its body.
  - `configure-ci-cd.md` (residual defect, found via `workflow-integration.md`'s redundancy check against it): the Exit Code Contract callout and its mermaid diagram both attributed ordinary path traversal (`Z202`, which stays Exit 1) to Exit 3, and omitted `Z204`/`Z205` from the Exit 2 row entirely — corrected on both counts.
- **`docs/how-to/troubleshooting.md` — Two Claims Corrected Against Live-Execution Evidence, Not Just Source Citations**:
  - External link validation (Pass 3) was stated to "run by default," with `--strict` described as unrelated to network requests. Live-tested against a real broken-external-link fixture across three flag combinations: without `--strict`, no network request is made at all and nothing is flagged; `--strict` alone triggers validation and correctly flags the broken link; `--strict --no-external` together skip it even under strict mode. Corrected to state the real gating condition (`_check.py`: `validate_links = strict and not no_external`) — this reverses a same-session correction that had itself introduced the inaccuracy.
  - The minimum required Zenzic Core version for the VS Code extension was stated as a fixed number (`v0.30.0`, itself a same-session correction of a previously stale `v0.23.1`). Confirmed the constant (`MIN_CORE_VERSION` in `zenzic-vscode`) is not tracked by any version-bump tooling — neither within `zenzic-vscode`'s own `.bumpversion.toml` nor across repositories — so a hardcoded number here would go stale again at the next `zenzic-vscode` release. Reworded to point at the extension's own live error message instead of restating a number that isn't kept in sync.
- **`Z204` (`FORBIDDEN_TERM`) Findings Mislabeled `[Z201]` in `check all`'s Per-Line Output and `--format json`**:
  - The `RuleFinding` list injected from `harvest()`'s combined credential/forbidden-term scan results (`scanner.py`'s `_scan_single_file`) unconditionally tagged every entry `rule_id="Z201"`, `message="Credential or secret detected: ..."` — including genuine Z204 forbidden-term hits, which showed as `[Z201]  Credential or secret detected: FORBIDDEN_TERM` in both the per-line listing and the `references` array of `--format json` output, instead of `[Z204]  Forbidden term detected — remove from documentation: '<term>'`. A separate, correct construction site building the top-level "POLICY VIOLATION DETECTED" banner already branched on `secret_type == "FORBIDDEN_TERM"` — this defect was in the per-line/JSON output only, not the banner. **Any tooling filtering or counting findings by `Z204` specifically (SARIF consumers, CI gates scoped to a specific code) would have silently missed every Z204 occurrence, since they were reported as `Z201`.** Fixed by mirroring the existing correct branch.
- **`.zenzic.toml` "Unknown Section"/"Unknown Key" Warnings Silently Dropped the Section/Key Name**:
  - The CLI's `RichHandler(markup=True)` logging setup interpreted the warning's `[%s]`-interpolated section name, and a literal `[section]` word in the same message, as Rich markup style tags — both were silently stripped from the rendered warning instead of printed. A misconfigured `.zenzic.toml` (e.g. a root-level setting accidentally nested under an unrecognized `[section]` header, one of the most common config pitfalls this warning exists to catch) produced a warning with the single most useful piece of information — which section is wrong — missing. Fixed by escaping the literal brackets in the message template and the interpolated value.
- **`zenzic init --local`'s "Zenzic Local Sandbox" Panel Printed a Literal `\n` Instead of a Line Break**:
  - All three gitignore-status lines appended after the panel's first line (the "additions made," "already protects," and "no Git repository detected" variants) ended in a double-escaped `"...\\n"` instead of a real `"\n"`, so the panel printed the two literal characters backslash-`n` instead of breaking to a new line before the closing "Edit local overrides safely..." sentence.
- **`zenzic init`'s Own Shipped Output Contained Phantom `/docs/`-Prefixed URLs**:
  - The "Next steps" panel's CI/CD-configuration link and the `[tool.zenzic]` header comment written into `pyproject.toml` by `zenzic init --pyproject` both pointed at `https://zenzic.dev/docs/...` URLs that 404 — mkdocs serves the site with `docs_dir` stripped from the path, same defect class as an already-fixed `README.md` issue earlier this release, except these instances were baked into the CLI's own source rather than a docs page. Corrected to `https://zenzic.dev/how-to/configure-ci-cd/` and `https://zenzic.dev/reference/configuration-reference/` respectively (the latter's page slug was also wrong, not just the prefix).
- **`Z203` (`PATH_TRAVERSAL_FATAL`) Had No `examples/` Fixture or `zenzic lab` Entry**:
  - Added the permanent `examples/z203-fatal-path-traversal/` gallery fixture and registered it in `zenzic lab`, closing a Mirror Law Target 8 gap — Z203's four Z2xx sibling codes (Z201, Z202, Z204, Z205) all already had both. Fixing this surfaced and closed a related gap in `zenzic lab`'s own scenario model: it previously had no way to represent an "expected security incident" (Exit 3) scenario, only "expected pass," "expected security breach" (Exit 2), and generic pass/fail — a correctly-firing Z203 scenario would always have shown as "expectation NOT met." `zenzic lab`'s internal `_Act`/`_ActResult` types now support a third `expected_incident` category.
- **`zenzic lab`'s Gallery Index Showed `FAIL` for `expected_pass=True` Scenarios**:
  - `_print_gallery_index()`'s `Expects` column branched `expected_breach` → `BREACH` and `expected_incident` → `INCIDENT`, but had no branch for `expected_pass` — the 4 real `expected_pass=True` scenarios (`Z112`, `Z123`, `Z401`, `Z620`) fell through to the `else` arm and displayed `FAIL` in the `zenzic lab` (no-argument) gallery menu, misleadingly labeling a "this configuration is fine" demo scenario as a failure before the user has run anything. Now shows `PASS`, matching the color convention already used elsewhere in the same file.
- **`docs/explanation/` Quadrant — First Batch (7 of 18 Pages) Corrected Against Actual Source and Live Execution**:
  - `community-index.md`: the LICENSE section's `pymdownx.snippets` include was wrapped inside an HTML comment (`<!-- --8<-- --> "LICENSE"`), which prevented the pattern from ever matching — the real Apache-2.0 license text was never actually included, only the literal dangling string `"LICENSE"` rendered. Corrected to the real bare `--8<-- "LICENSE"` syntax; live-verified the real license text now renders in the built site. The "Philosophy" card linked to `privacy-gate.md` (a security-architecture page, unrelated to design philosophy) instead of `brand-philosophy.md` — corrected.
  - `explanation/index.md`: the "Architecture Deep Dive" card described a "4-layer architecture" that doesn't match `architecture.md`'s own real "Three-Phase Pipeline" framing — corrected to the real terminology. The "Structural Integrity" card's teaser text ("Lossless AST compilation, VSM indexing, polyglot link extraction") described `core-mechanics.md`'s content, not `structural-integrity.md`'s actual content (which is about why Zenzic scopes itself to structural integrity/security rather than prose style) — corrected to describe the page it actually links to.
  - `configuration-loading.md`: claimed the repo root is located by walking up to a `.git` directory, `.zenzic.toml`, or `pyproject.toml`; the real implementation (`find_repo_root()`, `scanner.py:138-181`) never treats `pyproject.toml` as a root marker and additionally checks `zensical.toml` and `mkdocs.yml`, neither of which the page mentioned — corrected.
  - `privacy-gate.md`: the Z2xx security-family enumeration listed Z201-Z204 but omitted `Z205` (FORBIDDEN_SCHEME), despite it meeting every criterion the page uses to define family membership (same defect pattern already corrected once this release in `configure-ci-cd.md`) — corrected. Simplified the title from "Privacy Gate Architecture (Zero-Trust in CI/CD)" to "Privacy Gate Architecture."
  - `exclusion-design.md`: claimed auditing the effect of `respect_vcs_ignore` requires `--show-info`; that flag surfaces unrelated `info`-severity findings (e.g. `Z106` circular links), not exclusion effect — corrected to point at `zenzic config explain` (confirmed live: shows the resolved setting and its source, though not a per-file exclusion list). Trimmed a "Governance Score Math" section that duplicated `scoring-system.md`'s canonical `fail_under`/`suppression_cap` formula verbatim, replaced with a cross-link plus the exclusion-specific point that mattered (excluded paths cost nothing against `suppression_cap`; suppressed ones do). Added the missing `pyproject.toml [tool.zenzic]` Track-2 equivalent for `excluded_dirs`/`excluded_file_patterns`.
  - `baseline-tracking.md` needed no content correction — its two claims flagged `UNVERIFIED — needs live execution` by the audit (Exit 0/Exit 1 semantics under `--update-baseline`, and the exact resolution-hint string) were both independently live-verified accurate. That same live-verification pass surfaced a real, separate engine bug (not fixed in this release): `reporter.py`'s "FAILED: Hard errors detected. Exit code 1 is mandatory." text ignores baseline status entirely and can contradict the real exit code — see `.claude/state/03-priority-table.md` for the tracked follow-up.
- **`docs/explanation/` Quadrant — Second Batch (6 More Pages) Corrected or Confirmed Clean**:
  - `why-zenzic.md`: removed two boilerplate template-scaffolding sentences left in published copy ("This section details the specifications and guidelines for X within the Zenzic ecosystem") — the exact stub-content pattern Zenzic's own `Z501 PLACEHOLDER` check exists to catch. Corrected the "Defence Trinity" framing, which listed "Path and Topology Safety (Z202/Z203)" as a pillar separate from "Credential Leak Prevention (Z2xx)," when Z202/Z203 are themselves Z2xx codes, and claimed the whole Z2xx group is fail-closed and "immediately stops the pipeline" — untrue for `Z202`, which stays a plain exit-1 finding. Split into two accurately-scoped pillars (Z201/Z204/Z205 at exit 2; Z202/Z203 with their real, differing exit behavior). Aligned the "Three Pillars" section's wording with the canonical formulation in `docs/developers/explanation/governance/index.md`, which it had drifted from.
  - `scoring-system.md`: the "Structural Integrity" category code list omitted `Z112` (a live, current `structural`-category code) — added. The "CLI Quality Breakdown Ledger" worked example was fabricated: its own displayed numbers (a fully-zeroed Brand category) should trigger the Gravity Cap stage, but the example silently skipped it, understating the deduction by 1 point and omitting the real `Gravity Cap Enforcement` line the CLI actually prints. Replaced with a real `zenzic score` capture against a live fixture reproducing the same fully-zeroed-Brand scenario, with the real 3-point Gravity Cap deduction shown and explained.
  - `the-zenzic-trinity.md`: the `zenzic-doc` repository link pointed at the Core repo's own URL (a copy-paste error) — corrected to the real `zenzic-doc` URL. The claimed ADR "canonical format" (`title`, `**Status:**`, `## Context`, `## Decision`, `## Rationale`) doesn't match the actual 20-file ADR corpus (`**Status:**` present in only 9/20 files; `## Rationale` in 18/20) — corrected to describe what's actually universal (`## Context`, `## Decision`) versus what's common but not yet fully standardized.
  - `auto-fix-philosophy.md`, `language-server-architecture.md`, and `engine-migration-design.md` needed no correction — all technical claims confirmed accurate against source (`mutator.py`, `_fix.py`, `lsp/server.py`, `documents.py`, `adapters/_mkdocs.py`).
- **`docs/explanation/` Quadrant — Final Batch, Completes the First Full Pass (18/18 Pages)**:
  - `core-mechanics.md`: the Baseline Engine's signature formula was described as a full SHA-256 digest; the real implementation truncates to the first 16 hex characters (64 bits) — corrected.
  - `structural-integrity.md`: the Z202/Z203 finding-code table had swapped names, descriptions, and exit codes (showed Z202 as the fatal Exit-3 code named `PATH_TRAVERSAL_SUSPICIOUS`, a name that doesn't exist in the code registry) — corrected to the real Z202 (Exit 1) / Z203 (`PATH_TRAVERSAL_FATAL`, Exit 3) mapping. A "Pillar 2: Zero Subprocesses" link pointed at a page that never actually defines the Three Pillars — retargeted to the real definition.
  - `architecture.md`: the Exit-3 mermaid diagram branch was labeled `Z202` instead of `Z203` (the same conflation found across multiple pages this batch); a `.md`/`.md` typo (should read `.md`/`.mdx`) appeared in 3 places; the CLI package-layout table listed 11 files against 18 real ones — trimmed to a cross-link to the canonical, more-current `cli-architecture.md` rather than re-authoring a second copy; the `force_terminal` "always None" invariant was stated unconditionally, but it evaluates `True` when `FORCE_COLOR` is set — corrected; the Hybrid Adaptive Engine's parallel-execution threshold was stated as 50 files, the real value is 1000 (20x off) — corrected; a cited `examples/matrix/` directory doesn't exist — replaced with an accurate description of the real per-code `examples/` gallery; an Alias Mapping table had an empty header with no data rows — populated with the two real `@site/`-prefixed aliases.
  - `github-action-internals.md`: the Exit Code Contract table repeated the Z202/Z203 conflation (same pattern) and omitted `Z204`/`Z205` from the Exit-2 row; the architecture diagram showed `uvx zenzic check all`, but the action now provisions the tool once via `uv tool install` and invokes the bare binary from `PATH` — both corrected.
  - `discovery.md`: the heaviest-defect page in this quadrant. `respect_vcs_ignore`'s default was stated as `false`; the real default is `true`, and this page was the sole outlier against two other pages that already stated the correct default — corrected. The root-marker list was missing `zensical.toml`/`mkdocs.yml` (2 of the real 4 markers). A `.md`/`.md` typo and a false "skips symbolic links" claim were removed. The System Guardrails directory list included 2 non-existent entries (`__pycache__`, `.cache`) and omitted 9 real ones. The pattern-precompilation mechanism was misattributed to `fnmatch.translate()`; the real mechanism is a purpose-built RE2 translator. A "combined regex" VCS-pattern optimization was described that doesn't exist in source (VCS patterns delegate to the third-party `pathspec` library). The `inspect routes` `kind` field enumeration listed only 2 of the real 6 values. The MkDocs row of the engine-support matrix said a capability was unimplemented that is, in fact, already shipped. The Layered Exclusion precedence diagram placed CLI overrides last; the real evaluation order (per the module's own docstring) checks them third, before VCS-ignore and config exclusions — the mermaid diagram and prose were corrected to match. All corrections live-verified where the claim concerned CLI/build behavior: root-marker error text reproduced verbatim via a real `zenzic check all` run outside any project root; `mkdocs build --strict` re-verified clean after every change in this batch.
- **Stale "Docusaurus as a Current/Supported Engine" References — 4th Confirmed Instance of an Already-Known Pattern, Found and Fixed Site-Wide**:
  - `how-to/index.md`'s Engine Migration Guide card claimed readers can "migrate documentation seamlessly between MkDocs, Zensical, and Docusaurus" — verified against both the linked `migrate-engines.md` (which never mentions Docusaurus at all) and the real adapter registry (`_BUILTIN_ADAPTERS` in `_factory.py`: `mkdocs`, `zensical`, `standalone`, `prebuilt`, `vsm` — no `docusaurus` entry exists). Corrected to name only the two real migration targets.
  - `docs/developers/explanation/adr-vault/index.md`'s "Documentation Site Decisions" section described `zenzic.dev` in present tense as "the Docusaurus site" — the site has been built with MkDocs since the Docusaurus adapter's removal in `v0.14.0`. Reworded to past tense, consistent with how the one ADR under that heading already frames itself as historical.
  - `docs/developers/explanation/adr-vault/records/adr-lint-source.md` (ADR 001, Status: Active) claimed in present tense that "the same `check links` command validates ... a Docusaurus site" — corrected to name the real, currently-supported engines, with a historical note on the removed one.
  - `docs/developers/explanation/adr-vault/records/adr-agnostic-universalism.md` (ADR 005, Status: Active) still listed Docusaurus as one of the currently-implemented `Z404` engine checks in its decision table — added a historical-context note (matching the established pattern already used by sibling ADR 006) and marked the Docusaurus table row as removed/historical, without altering the ADR's still-valid core decision for the remaining engines.
  - `docs/how-to/install.md`'s "no build runtime required" example named `(MkDocs, Hugo, Docusaurus)` as illustrative build engines — neither Hugo nor Docusaurus was ever a genuine Zenzic adapter; corrected to name real engines.
  - Not flagged as stale, with reasoning: `docs/explanation/architecture.md`'s "Docusaurus-style alias" reference (describing the `@site/` alias naming convention's origin, not a support claim), the `adr-unified-perimeter.md`/`adr-bilingual-structural.md` ADRs (already correctly self-marked `[DEPRECATED - HISTORICAL ARCHIVE]`), and `docs/_redirects`' Docusaurus-era redirect rules (legitimate URL-migration routing, not content claims).
- **`docs/developers/` Quadrant — First Remediation Batch (4 Pages) Corrected Against Actual Source**:
  - `index.md`: the "Interactive Workflow with Just" table listed stale/incomplete recipe descriptions — rewritten against the real `justfile` (`sync`, `check`, `test`, `test-full`, `verify`'s 5 real steps, `docs-build`, `docs-serve`, `check-badges`, and `clean`'s real removed paths — confirmed `site/` is **not** among them). The "Mutation testing" callout named only 1 target file; corrected to name all 3 real `[tool.mutmut]` targets (`rules.py`, `credentials.py`, `reporter.py`).
  - `explanation/core-laws.md`: the "Zero I/O in the Hot Path" table's Pass 1 row named a non-existent `validate_links_async` function and claimed an `rglob` traversal — the real current entry point is `scan_docs_references`, and the real traversal mechanism is `walk_files()` (`os.walk`-based, explicitly documented as a deliberate non-`rglob` alternative in `discovery.py`'s own docstring). Corrected.
  - `explanation/sovereign-verification-model.md`: the Core Resolution Topology diagram labeled the fail-closed stop "Exit 1" — the real value is Exit 2, cross-verified against `zenzic-action/justfile` and `zenzic-vscode/justfile`'s identical "Shared sovereign model" fail-closed branch (both literal `exit 2`). Corrected.
  - `explanation/governance/licensing.md`: the `REUSE.toml` example was entirely fabricated (a `docs/**`/`i18n/**`/`*.md` bulk-declaration block that does not exist in the real file) and its coverage-strategy table wrongly claimed documentation pages use `REUSE.toml` bulk declaration — the real mechanism is a per-file inline SPDX header (this page's own header is a live example). Replaced the fabricated example with real excerpts from `REUSE.toml` and corrected the table. Also corrected the pre-commit hook numbering: REUSE lint runs as its own dedicated hook (`reuse`, hook 5 of 8), not as part of the Zenzic Guard hook (hook 7 of 8 — an unrelated secret scanner) as previously stated; `just-verify` (hook 8 of 8, pre-push only) transitively re-runs the full commit-time chain, `reuse` included.
  - Documentation only — no code behavior changed. First 4 of ~40 pages in this quadrant; remainder tracked in `.claude/state/03-priority-table.md`.
- **`docs/developers/` Quadrant — Second Remediation Batch (7 Pages, 9 Structural Findings) Corrected Against Actual Source**:
  - `explanation/governance/exit_strategy.md`: Section 2 claimed the adapter contract uses `typing.Protocol` structural subtyping ("no MRO to audit," "you do not need to subclass a Zenzic base class") — the real, ADR-078-STRICT-hardened contract is `BaseAdapter(ABC)` with `@abstractmethod` methods, a nominal inheritance relationship. Rewrote the section around the real contract, distinguishing built-in-engine users (no Python code at all) from custom-adapter authors (one small file to delete on removal, not an inheritance chain through application code); removed a fabricated `get_docs_root()` method reference (no such method exists anywhere in `src/`). The Zero Residue table's "Your Python types" row corrected to match.
  - `explanation/index.md`: the teaser claimed "the 8-step protocol for introducing new diagnostic finding codes" — the real Mirror Law protocol (ADR-020) is 10 mandatory targets. Corrected and cross-linked to `how-to/write-a-check.md`'s own protocol section, which was itself missing 2 of the 10 targets (`mkdocs.yml` nav registration was folded into another bullet instead of listed separately; `src/zenzic/cli/templates.py` and the `zenzic-vscode` JSON Schema targets were missing entirely) — brought in sync in the same pass.
  - `explanation/adr-vault/records/index.md`: merged into the parent `adr-vault/index.md` — its content was a near-duplicate of the parent's own introduction with no unique value. Removed the `mkdocs.yml` nav entry, added new `_redirects` lines for the bare directory URL (none existed before, since the URL previously served a real page), and repointed 4 individual ADR pages' `./index.md` links to the parent.
  - `explanation/core-laws.md`: the "Ghost Route Awareness" ✅-correct worked example used `RouteStatus.ORPHAN_AND_ABSENT`, which does not exist — `RouteStatus` is a 4-member `Literal` type, not an `Enum`. Rewrote the example around the real `route_info.status == "REACHABLE"` check and explained why Ghost Routes need no special-casing (they're injected into the VSM as ordinary `REACHABLE` routes).
  - `explanation/mdx-asset-rationale.md`: the page's entire technical premise (mandating `.tsx` React components with `<Translate>` i18n wrappers) contradicted its own linked SSoT (`reference/zenzic-style.md` §10, which mandates native HTML/Jinja templates or Mermaid code blocks) and the fact that zero `.tsx` files exist anywhere in the repository. Fully rewritten around the real mechanism (inline HTML with the site's real CSS custom properties, or Mermaid code blocks) — this project has no React/JSX toolchain and no `mkdocs-macros-plugin`, so true Jinja variable interpolation inside a Markdown page's body was never possible either. `.claude/references/03-dqs-and-mirror-law.md`'s own matching `.tsx` citation corrected in the same pass.
  - `explanation/governance/technical-debt.md`: the ledger's sole "Open Entry" cited `Z108` `STALE_ALLOWLIST_ENTRY` — `Z108` is actually the unrelated, already-shipped `EMPTY_LINK_TEXT`; the real `STALE_ALLOWLIST_ENTRY` feature already shipped as `Z112`. Moved to "Closed Entries" under its real code identity, with a factual account of how the original "Pillar 3" deferral concern was actually addressed (an aggregation pass over already-collected per-file data in the main process, not a per-worker redesign); an uncorroborated "ADR 011 (removed in v0.8.0)" citation was dropped rather than carried forward unverified. "Open Entries" — previously boilerplate template scaffolding with no real content — now names a genuine, already-disclosed current gap (the CLI/LSP topology-model divergence between `Z402` and `Z410`/`Z411`, cross-referenced from this same CHANGELOG's own "Known Limitations" section) instead of being left empty or stale.
  - `explanation/tailwind-mkdocs-bridge.md` and the companion blog post `blog/posts/2026-06-20-tailwind-mkdocs-material-bridge.md`: both described the rem-scaling fix as `html:has(.zz-tailwind-root) { font-size: 100% }` — the real shipped CSS is `.zz-tailwind-root { font-size: 16px !important; }`, scoped directly to the wrapper (not `<html>`) specifically to avoid resetting the MkDocs Material header/sidebar/TOC that live outside the wrapper. Both pages rewritten around the real mechanism and the real reason `:has()` was deliberately not used for this particular rule (it genuinely is used elsewhere on the same page, for an unrelated header-widget-hiding toggle, where its page-wide reach is exactly what's wanted). The `:has()` browser-support figures (Chrome 105+, Firefox 121+, Safari 15.4+) were independently live-verified against caniuse.com and found accurate as originally stated — no correction needed there.
  - `explanation/governance/lpgp.md`: an internal 4-section homepage plan, substantially superseded by the real, much larger 8-partial homepage structure that shipped, with no framing explaining the mismatch. Added a dated "Historical — superseded" note rather than rewriting the plan to match the current homepage, matching the established ADR-021 historical-snapshot precedent; the plan is preserved as a point-in-time record, not kept in sync going forward.
  - `explanation/governance/index.md` and `explanation/governance/evolution_policy.md`: re-confirmed still accurate, no changes needed.
  - Documentation only — no code behavior changed. Completes all findings from the first 3 `docs/developers/` audit batches (Batches 0-2); the remaining ~27 batches are tracked in `.claude/state/03-priority-table.md`.
- **`docs/developers/explanation/adr-vault/records/` Missing an Index Page — Self-Inflicted `Z401` Regression From the Just-Completed Merge**:
  - Merging `records/index.md` into the parent `adr-vault/index.md` (previous entry) correctly removed duplicated content, but left the `records/` directory with Markdown files and no index page — `Z401` (`MISSING_DIRECTORY_INDEX`) fired for the directory (live-verified before and after: baselined info-level finding present, then gone). Added a genuinely minimal `records/index.md` stub (not a re-duplication of the parent) pointing readers to `../index.md` as the real entry point, matching the established `how-to/index.md`/`reference/index.md` stub pattern; registered in `mkdocs.yml` nav to avoid trading `Z401` for a new `Z402` orphan-page finding. Two of the eight `_redirects` lines added by the merge (the bare, unprefixed `records/` URL) were retargeting a URL that now serves this real page again — corrected: those 2 lines removed (a redirect FROM a URL that now has real content would have prevented that content from ever being reached), and the remaining 4 `/docs/`-/`it/`-prefixed historical variants retargeted from the parent page back to the restored `records/` page, matching this file's own established locale/legacy-prefix normalization convention.
- **Root `.zenzic.toml`'s `docs_dir` Comment Described a Non-Existent "Auto-Discovery" Mechanism, Citing the Removed Docusaurus Engine**:
  - The comment claimed that leaving `docs_dir` unset triggers engine-based auto-discovery ("reads `docusaurus.config.ts`, `mkdocs.yml`…") — this conflated two unrelated mechanisms: `docs_dir`'s real default is a fixed `Path("docs")` (`models/config.py`, no discovery involved at all), while `discover_engine()` (a *different* field, `engine`, not `docs_dir`) genuinely does probe config files, but only `.zenzic-vsm.json`/`zensical.toml`/`mkdocs.yml`/`mkdocs.yaml` — `docusaurus.config.ts` has never been part of that list post-removal, and Docusaurus itself was removed as a supported engine in v0.14.0. Traced via `git log -S`: this exact comment text was introduced at v0.8.0 (2026-05-30), predating the v0.14.0 removal — accurate when written, never updated after. Corrected to describe the real fixed-default behavior, matching `src/zenzic/cli/templates.py`'s own `zenzic init` scaffold text, which was already accurate and did not need a matching fix — the drift was isolated to this one committed file, not template-reproduced.
- **`mkdocs.yml` Silently Emitted an Undeclared "Pages Not in the Nav" `INFO`-Level Warning for All 21 Archived ADR/Governance Pages on Every Build**:
  - Live-verified via a full, unpiped `mkdocs build --strict` run (not the `tail`-truncated capture a prior directive's verification used, which cut off the warning's header line and left it looking clean) that MkDocs' own native `validation.nav.omitted_files` check — independent of any Zenzic-side code — fires at `INFO` level for every page that exists on disk but isn't in `nav:`. It doesn't fail the build (MkDocs' strict-mode `CountHandler` only counts `WARNING`-and-above; `omitted_files` defaults to `info`, confirmed directly against the installed `mkdocs==1.6.1` source), so the exit code was genuinely 0 all along — but the warning text itself was real, present in full on every build, for the 20 ADR records plus `release-governance-protocol.md` moved out of nav by this session's `directory_policies` work. `mkdocs.yml` already ships the exact declared-exemption mechanism for this (`not_in_nav:`, a gitignore-style pathspec — previously used only for `includes/*`) but the 21 archived pages were never added to it, so MkDocs kept treating their omission as unintentional. Added `developers/explanation/adr-vault/records/*` and `developers/how-to/release-governance-protocol.md` to `not_in_nav:`, matching the same "declare it, don't hide it" discipline already applied to the `Z402`/`Z103` `directory_policies` exemptions for the identical set of pages. No exit-code or DQS-score change (the build was already passing) — the fix is that the build's own diagnostic output is now honest about a status that was already true, instead of surfacing an un-declared warning that a piped/truncated capture could silently miss.
- **`docs/developers/how-to/implement-adapter.md` and `write-plugin.md` Both Contained Worked `BaseAdapter`/`BaseRule` Examples That Raise a Real `TypeError` If Copy-Pasted**:
  - `implement-adapter.md`'s `MyEngineAdapter` example omitted `get_entry_points(vsm)`, a required `@abstractmethod` on `BaseAdapter` (`adapters/_base.py`) — live-reproduced the exact `TypeError: Can't instantiate abstract class ... without an implementation for abstract method 'get_entry_points'` a reader would hit by following the page's own "Testing Your Adapter" section. Added a correct implementation (nav-restricted entry points, falling back to every route when there is no nav tree, mirroring the real `MkDocsAdapter`/`StandaloneAdapter` implementations), a new invariant (#13) in the Adapter Contract Guarantees list, and a new row in the Metadata-Driven Routing table. `write-plugin.md`'s `check_vsm` example declared only 4 parameters after `self` (`file_path, text, vsm, anchors_cache`); the real `BaseRule.check_vsm` signature and its only call site (`core/rules.py:371-378,674`) both pass a 5th, `context: ResolutionContext | None` — live-reproduced the exact `TypeError: ...check_vsm() takes 5 positional arguments but 6 were given` the engine would raise calling the doc's version, fixed by adding the missing parameter. Both fixes live-verified: the pre-fix code reproduces the real `TypeError` against the actual installed engine, the post-fix code runs clean, including 4 new example-behavior tests for `get_entry_points`.
- **`docs/developers/how-to/troubleshooting.md`'s `Z105` Guidance Was Backwards, and Its Suppression Example Used a Syntax That Doesn't Match the Real Suppression Regex**:
  - The page told readers that Zenzic blocks *relative* traversal paths and recommends *absolute* site-root paths as the fix — the real, live-verified behavior is exactly the opposite: `Z105` fires on absolute paths (e.g. `/guide`) and the correct fix is a relative path, because an absolute path silently mis-resolves when a site is hosted under a subdirectory. Corrected the Symptom/Resolution text. Separately, the page's suppression example (`<!-- * zenzic:ignore: Z105 ... * -->`) used asterisks that don't match the real suppression-comment regex, and placed the comment on its own line above the link rather than at the end of the link's own line (the real, documented convention — `suppression-policy.md:77`, "placed at the end of a line"). Fixed both: correct syntax, correct same-line placement, live-verified against the real engine to actually suppress the finding. Also added an `absolute_path_allowlist`-based alternative for the case where many links legitimately need to stay absolute against a fixed prefix.
- **`docs/developers/reference/credential-scanner-obligations.md`'s Obligation 2 Described a Fabricated `SIGALRM`-Based "Regex-Canary" Mechanism That Does Not Exist**:
  - The page documented a `_assert_regex_canary()` function, `_CANARY_STRINGS`/`_CANARY_TIMEOUT_S` constants, and a POSIX-`SIGALRM`-based runtime stress test — zero matches anywhere in `src/`, confirmed by full-repo grep; the cited post-mortem `docs/internal/security/shattered_mirror_report.md` also does not exist. The real mechanism (`CustomRule.__post_init__`, ZRT-007) is structurally different and stronger: every custom-rule pattern is compiled with Google RE2 (a DFA engine with a hard linear-time guarantee) at rule-construction time; a pattern RE2 cannot represent as a DFA — a backreference or a lookaround — is rejected immediately as a `PluginContractError`, before any scan runs. Rewrote the section around the real mechanism, live-verified against the installed engine: the doc's own former "Patterns to avoid" table (`(a+)+`, `(a|aa)+`, `(a*)*`, `.+foo.+bar`) was **also wrong** — all four are accepted by RE2 and run in sub-millisecond time even against a pathological input, since RE2 never backtracks regardless of pattern shape; only backreferences and lookarounds are genuinely rejected, and the page's tables were corrected to reflect that. Removed the fabricated Windows/POSIX platform-support caveat along with it, since RE2's compile-time rejection has no such platform dependency. Documentation only — no code behavior changed.
- **`ZenzicRuleV3._execute_ast_visitors()` Never Dispatched `visit_code_block` — a Custom Rule SDK v3 Hook Was Publicly Documented but Completely Non-Functional**:
  - `visit_code_block` has been publicly documented since v0.28.0 (`write-ast-rule.md`, `CONTRIBUTING.md:78`, `changelogs/v0.28.x.md:36`) as one of five SDK v3 visitor hooks, but `_execute_ast_visitors()` only ever checked and dispatched `visit_link` and `visit_heading` — a custom rule overriding only `visit_code_block` silently received zero callbacks regardless of code-fence content, with no error or warning. Added dispatch: a new `_extract_code_blocks()` helper walks fenced code blocks using the same fence-matching pattern as `core.rules.UntaggedCodeBlockRule` (imports its `_FENCE_OPEN_RE` directly rather than re-declaring an equivalent regex, to avoid a second maintenance surface for what counts as an opening/closing fence), yielding `(start_line, lang, code)` for each block; `_execute_ast_visitors()` now calls `visit_code_block` for every block when the hook is overridden. TDD evidence: a new `CustomCodeBlockRule` + `test_sdk_v3_visit_code_block` reproduces the exact bug (`assert 0 == 1` red state against the pre-fix code); green state after the fix (5/5 SDK v3 tests passing, full suite 2101 passed). No public API signature changed — `visit_code_block`'s documented signature was always correct; only the dispatch that was supposed to call it was missing.
- **3 `docs/developers/how-to/contribute/` Pages Described GitHub Issue Templates That Don't Match the Real `.github/ISSUE_TEMPLATE/*.yml` Forms**:
  - `report-a-bug.md` and `request-a-change.md` both described a generic, free-form "Title / Context / Description / Related links / .../ Checklist" template — but the real templates (`bug_report.yml`, `feature_request.yml`) are structured GitHub Issue Forms with specific typed fields (dropdowns, required/optional textareas) that share no field names with the doc's description. Rewrote both sections around the real fields: `report-a-bug.md` now describes `bug_report.yml`'s Zenzic/Python version, OS, documentation engine, command run, expected/actual behaviour, `zenzic.toml`, and alert-output fields; `request-a-change.md` now describes `feature_request.yml`'s feature category, problem to solve, proposed solution, alternatives considered, and the Zenzic design-pillars self-check. `report-a-docs-issue.md` described a template that doesn't exist at all — no dedicated docs-issue GitHub template is defined — corrected to say so plainly and direct readers to open a blank issue instead, keeping the same underlying writing guidance (title, description, related links, proposed change) as structure for that blank issue rather than presenting it as a real template's fields. `request-a-change.md` also had a stale product-description sentence ("static content analysis framework for Markdown documentation") predating the current "Deterministic Document Integrity Engine" branding (`pyproject.toml:17`, `docs/index.md:3`) — corrected. Documentation only.
- **`pull-requests.md` Told Contributors to Run `uv sync --group docs`, a Dependency Group That Does Not Exist**:
  - The MkDocs stack is a `[project.optional-dependencies]` PEP 508 extra (`pyproject.toml:82-83`), not a `[dependency-groups]` member — the real, live-verified command is `uv sync --extra docs`. `uv sync --group docs` fails outright (no such group). Corrected the command and the preceding dependency-groups table, which had incorrectly listed `docs` as a fifth group alongside `test`/`lint`/`release`/`dev`.
- **3 `docs/developers/reference/` Pages Cited Nonexistent Functions, a Missing 11-of-14 `BaseAdapter` Method Contract, a Missing 5-of-18 CLI Module List, and a Wrong Module Path for `ZenzicPalette`/`ZenzicUI`**:
  - `adapter-api.md`: the `zenzic.core.scanner` mkdocstrings block cited `find_placeholders` and `check_placeholder_content` — zero matches anywhere in `src/`; the real Z501 placeholder check is `PlaceholderRule` in `core.rules`, not a scanner function. Also cited `find_config_file` under `zenzic.core.scanner`, but it actually lives in the internal `adapters/_mkdocs.py` module — removed (this page documents public modules only). `find_repo_root`'s prose description was stale on two counts: it named only 2 of the real 4 root markers (`.git/`, `.zenzic.toml`, `zensical.toml`, `mkdocs.yml`) and omitted the real `search_from` parameter — corrected to match the real signature and docstring. The `BaseAdapter` "Core Methods" section documented only 4 of the interface's 14 real `@abstractmethod`s (plus 3 of its 4 optional/default members) — expanded to cover all 14 abstract methods and all 4 optional members, cross-linked to `implement-adapter.md`'s Adapter Contract Guarantees for the full behavioral invariants.
  - `cli-architecture.md`: the Module Map table listed 12 of the real 18 files in `src/zenzic/cli/` — missing `_audit.py` (`audit` command), `_env.py` (`env` command), `_fix.py` (`fix` command), `_lsp.py` (`lsp` command — starts the Language Server), and `templates.py` (`zenzic init` scaffold text). Added all 5 rows, verified against the real directory listing.
  - `zenzic-style.md`: §8/§9 cited `src/zenzic/ui.py` and `from zenzic.ui import ...` for `ZenzicPalette`/`ZenzicUI` — that module path does not exist; live-confirmed the real path is `src/zenzic/core/ui.py`, matching `cli-architecture.md`'s own correct citation of the same classes. Corrected all 6 occurrences, including inside the page's own "forbidden pattern" code examples (so those illustrate only the intended anti-pattern — a removed flat colour constant — without also demonstrating an unrelated wrong-module-path bug). §7's checklist also referenced a "§2" that didn't exist anywhere in the document (real heading sequence was 1→3→4→…→10) — rather than a bare renumbering, authored the missing content: a new §2 "Admonition Role Taxonomy," grounded in a real site-wide grep of all 8 admonition types actually in use (`note`/`tip`/`info`/`danger`/`warning`/`abstract`/`caution`/`important`) and their observed real usage patterns, not an invented scheme.
  - Documentation only — no code behavior changed.
- **`_check.py`'s Internally Inconsistent "Seven Checks" vs. "Six Named Checks" Docstrings Both Undercounted the Real Number**:
  - `_collect_all_results()` said "seven checks"; the public `check_all` command named 6 by name ("links, orphans, snippets, placeholders, assets, references"). Traced the real count directly from `_AllCheckResults`'s 8 non-derived fields and each field's construction site: link validation, orphan detection, snippet validation, unused-asset detection, nav-contract validation (`Z406`), directory-index validation (`Z401`), config-asset validation (`Z404`), and the combined reference/rule-engine/security pipeline (`scan_docs_references`, which itself covers `Z2xx` security, `Z3xx` references, `Z5xx` content — including placeholders — and `Z6xx` brand rules in one pass). The real number is **eight** — both docstrings undercounted, and the public one also silently omitted 3 checks (`Z406`, `Z401`, `Z404`) that have no standalone `zenzic check <name>` sub-command at all, reachable only via `check all`. Corrected both docstrings, a related code comment, and 3 docs pages that mirrored the same stale counts/lists (`docs/reference/cli.md`'s `check all` table row, which now also calls out the 3 sub-command-less checks explicitly; `docs/explanation/discovery.md`; `docs/developers/how-to/contribute/pull-requests.md`). `tests/test_cli_help_snapshot.py`'s `test_check_all_help_snapshot_contract` and its `tests/snapshots/help_check_all.txt` fixture updated to match the real, corrected `--help` text (live-captured from the actual CLI, not hand-written). No behavior change — docstring/comment/docs accuracy only.
- **`@site/static/` Asset Links Reported a False `Z104` Even When the Asset Genuinely Existed**:
  - Root-caused to `incremental.py`'s `_run_urp_checks()` non-markdown asset-existence check, the sole `Z104` construction site: it joined `path.parent / rel_url` directly, never applying `@site/`-alias resolution at all, so `@site/static/logo.png` was checked against a nonexistent `<linking file's dir>/@site/static/logo.png` instead of the real `repo_root/static/logo.png`. Fixed by extracting the alias-resolution logic already used by markdown-link resolution (`InMemoryPathResolver._build_target`, `resolver.py`) into a new shared, pure, string-based function `resolve_href_target()`, and calling it from `_run_urp_checks()` too, so the CLI/LSP-shared incremental engine and the markdown-link resolver now agree on what an `@site/` alias means instead of one of them re-deriving it independently. `InMemoryPathResolver._build_target` now delegates to the same function (behavior-preserving, verified via the full resolver test suite unchanged). `tests/integrity/test_i18n_path_integrity.py::test_int_004_site_static_asset_resolves_from_locale_file` (previously `xfail(strict=True)`) now passes for real; marker removed.
- **A Crashing Adapter Took Down the Entire `zenzic check`/`zenzic-mcp` Process With a Raw Traceback Instead of a Clean Error**:
  - `get_adapter()` (`_factory.py`) — the single choke point used by every CLI command, `scanner.py`, `validator.py`, and the LSP server, and also imported directly by `zenzic-mcp` — had zero exception handling around adapter class construction and `has_engine_config()`. A bug in a third-party or built-in adapter (an exception in `__init__`/`from_repo`, a structurally incomplete subclass) propagated as a raw, unhandled Python exception; `cli_main()`'s top-level handler only catches `ZenzicError`/`PluginContractError`, so this produced a raw traceback rather than a reported error. Wrapped both call sites: a `ZenzicError` subclass (e.g. the real `ZensicalAdapter` raising `ConfigurationError` when `zensical.toml` is absent) still propagates unchanged; any other exception is now re-raised as `CheckError` ("check machinery failure, not a finding" — already an existing, already-`cli_main()`-caught error class, no new taxonomy introduced), rendering as a clean error panel instead of a crash. Exit code is unchanged (still 1 in both cases), so `zenzic-action`'s wrapper — which parses only numeric exit codes, never traceback text — is unaffected. `zenzic-mcp`'s `analysis.py` imports `get_adapter` directly but its `server.py` tool handler doesn't specifically catch either the old or new exception type, so the exception now surfaces with a clearer message but is otherwise unchanged there too — a separate, pre-existing gap in `zenzic-mcp`'s own error handling, logged in `03-priority-table.md`, not fixed here (out of this repo's scope, and not small/unambiguous without knowing the MCP SDK's own outer exception semantics).
- **`docs/reference/suppression-policy.md`'s Suppression Cascade Diagram Still Implied All of `Z201`-`Z205` Escalate to Exit 2/3, Contradicting Its Own Table Directly Below**:
  - The page's Exit Code table (`Z202` → Exit 1, not escalated; `Z203` → Exit 3, fatal) was already corrected in an earlier pass this session, but the mermaid diagram above it still labeled the shared "Z201-Z205 Security Breach" branch as `"FATAL SECURITY OVERRIDE (Exit 2/3)"` — technically wrong for `Z202` specifically, which never escalates past Exit 1. Relabeled to `"SECURITY OVERRIDE (Non-Suppressible)\nExit code varies by code — see table below"`, removing the incorrect specific exit-code claim from the diagram rather than duplicating (and risking re-drifting) the table's own precise per-code breakdown. `mkdocs build --strict` and `just check` (98/100, 0 new) clean after the fix.
- **`docs/reference/cli.md`'s Exit Codes Section Was Unfilled Boilerplate Scaffold Text With No `Exit 3` Subsection at All**:
  - The section promised "Each exit code has a distinct visual signature," then only provided `### Exit 0`/`### Exit 1`/`### Exit 2` subsections, each an unfilled template line ("This section details the specifications and guidelines for Exit N...") with an empty ``<!-- Terminal output: run `uvx zenzic check all` -->`` comment — no captured output, and no `### Exit 3` subsection despite the table two lines above documenting it. Replaced all 3 stubs and added the missing 4th with real output, live-captured from the actual CLI against 4 purpose-built fixtures (a clean 2-page site; a broken internal link; a leaked AWS credential; a path-traversal link to `/etc/passwd`) — not written from memory. `mkdocs build --strict` and `just check` (98/100, 0 new) clean after the fix.
- **`docs/rules/Z204.md`'s Configuration Section Showed Live Regex Syntax in the Committed `.zenzic.toml`/`pyproject.toml`, Directly Contradicting `configure-privacy-gate.md` and the Real Engine Behavior**:
  - `forbidden_patterns` values are matched as literal, case-insensitive substrings — regular expressions are explicitly not supported, every pattern is escaped before compilation (confirmed live against `src/zenzic/core/credentials.py`) — and are intended to live in the git-ignored `.zenzic.local.toml`, not the shared, committed `.zenzic.toml`. `Z204.md` showed the opposite of both: word-boundary/case-insensitive-flag regex syntax (`'(?i)\bProjectOmniInternal\b'`) declared in `.zenzic.toml`/`pyproject.toml [tool.zenzic]`. Every other page in `docs/` mentioning `forbidden_patterns` (8 checked) already correctly used literal strings and `.zenzic.local.toml` — `Z204.md` was the sole outlier. Rewrote the Rationale and Configuration sections to match, live-verified against the real engine (a literal `ProjectOmniInternal` term in `.zenzic.local.toml` correctly triggers `Z204`, Exit 2). **Second, related instance found and fixed in the same pass**: `docs/explanation/github-action-internals.md`'s Secret Guard Step section claimed the `guard scan` step reads `forbidden_patterns` "from the repository's `.zenzic.toml`" only — confirmed via `_guard.py:88,161` that `guard scan` calls the exact same `ZenzicConfig.load()` as `check all`, which merges in `.zenzic.local.toml` — corrected to describe the real, shared config-loading path. `mkdocs build --strict`, `just check` (98/100, 0 new) clean.
- **`github-action-internals.md`'s Guard-Scan Correction (Above) Was Itself Incomplete — `.zenzic.local.toml` Is Not Actually Present in a Typical CI Run**:
  - The immediately preceding fix correctly identified that `guard scan` calls the same `ZenzicConfig.load()` as `check all`, which merges in `.zenzic.local.toml` when present — but didn't address whether that file is actually *present* in CI, the more practically important question for a reader. Live-confirmed: `.zenzic.local.toml` is genuinely git-ignored in this repo (`.gitignore:16`) and does not exist in a checked-out working tree; `zenzic-action`'s `action.yml` and wrapper script have zero references to `.zenzic.local.toml` or any secret-to-file injection mechanism (repo-wide grep, zero hits) — there is no built-in way for the action to provision it. `docs/how-to/configure-privacy-gate.md` already documents this accurately ("In CI, `forbidden_patterns` is typically empty... unless you explicitly provision patterns via a CI secret," with a manual workflow-step example). Corrected `github-action-internals.md` to state the same real behavior — `forbidden_patterns` is typically empty in CI unless a workflow explicitly writes the file from a secret in an earlier step, cross-linked to the how-to page rather than duplicating its CI example. `mkdocs build --strict`, `just check` (98/100, 0 new) clean.
- **`github-action-internals.md` and `zenzic-action.md` Described a Fictional 3-Tier `.github/.zenzic.toml` Config-Discovery Cascade, and Were Also Wrong About Current Fatal-Error Behavior**:
  - Both pages' "Root-First Discovery"/"Sovereign Intent Contract"/"Configuration Discovery" sections described the config-file feature's *original design* (an auto-discovery cascade through `config-file` → `.zenzic.toml` → `.github/.zenzic.toml` → built-in defaults, with a `strict`-mode-dependent warning-vs-error branch for a missing explicit file) — but that design was replaced when the feature was actually implemented for real (see the `--config PATH` entry earlier in this file). Live-confirmed directly against `zenzic-action-wrapper.sh`'s real config-file sandbox guard: there is no `.github/.zenzic.toml` tier at all (repo-wide grep, zero hits) — when `config-file` is unset, the wrapper passes nothing and `zenzic`'s own normal discovery chain applies; when set but the file doesn't exist, the step **fails unconditionally** (`::error` + `exit 1`), with **no `strict`-mode branching** — directly contradicting both pages' own tables, which claimed `strict: false` degrades to a warning-and-continue path. Rewrote all 3 affected locations in `github-action-internals.md` (`Config Jailbreak Guard`'s note, and the `Root-First Discovery`/`Sovereign Intent Contract` sections merged into one accurate `Explicit Config Override` section) and the 3 locations in `zenzic-action.md` (the Inputs table's `config-file` row, the `Configuration Discovery` table, its `Sovereign Intent Contract` note) to match the real, simpler, already-correct behavior `zenzic-action`'s own `README.md`/`action.yml`/`CHANGELOG.md` have described since the feature shipped — confirming the fiction was isolated to `zenzic` core's own docs *about* `zenzic-action`, not duplicated in `zenzic-action` itself. **Confirmed via `zenzic-action`'s own git history, not assumed from current behavior alone**: `config-file` had zero handling anywhere in the wrapper before it was wired up for real (`78cafe3`, 2026-08-28) — that commit's own message states the always-fatal design directly ("A nonexistent config-file now fails the step instead of silently falling back to normal discovery"). No earlier wrapper version ever implemented the conditional fallback the stale docs described; it was aspirational documentation for a design never built, not a regression from one that was. `mkdocs build --strict`, `just check` (98/100, 0 new) clean.
- **`Z204` (`FORBIDDEN_TERM`) Was Never Detected via the LSP/Real-Time-Editor Path — Only a Full `zenzic check all` CLI Scan Caught It**:
  - `IncrementalAnalysisEngine` (`incremental.py`) — the shared analysis primitive behind the LSP server (`zenzic-vscode`'s real-time diagnostics) and `zenzic-mcp`'s `check_document` tool — only ever called `scan_lines_with_lookback()` (the credential/secret scanner, `Z201`) in its security-scan pass. It never called `scan_line_for_forbidden_terms()`, the function that actually detects `Z204`. A forbidden term configured in `.zenzic.local.toml` (e.g. an internal codename that must never appear in published docs) produced a real diagnostic during a full CLI scan, but **no LSP diagnostic at all** while editing the same file — a silent security-adjacent coverage gap between the two paths, not a mislabeling. Fixed by mirroring `scanner.py`'s `harvest()` first-match-wins pattern in `_analyze_file()`: lines already flagged as a credential are skipped for the forbidden-term pass, so a line can't be double-reported as both `Z201` and `Z204`. TDD evidence: 2 new tests in `test_incremental_engine.py`, both confirmed red (no `Z204` diagnostic at all) before the fix, green after; a genuine implementation subtlety surfaced and was fixed in the test itself along the way — `process_changes()` reloads config from disk on every call (an intentional hot-reload feature for live `.zenzic.toml` edits), so a config object passed only to the engine's constructor is silently overwritten unless the setting also exists in a real file on disk, which the fixed tests now do. Full suite green (2124 passed, 2122→2124), `ruff`/`mypy` clean. **Cross-repo impact**: `zenzic-vscode` and `zenzic-mcp` both consume `IncrementalAnalysisEngine` directly and were grepped for any existing claim about `Z204`/`forbidden_patterns` coverage — zero hits in either repo's docs, code, or tests, so no correction was needed anywhere; both simply gain correct `Z204` detection automatically once they pick up the fixed core version, with no code change of their own required.
- **`docs/reference/zenzic-action.md`'s Exit Code Contract Table Said Only `Z201` Triggers Exit 2, Omitting `Z204`/`Z205`**:
  - Found while checking whether `github-action-internals.md`'s Exit Code Contract table duplication with this page had been addressed by an earlier fix in this same pass — it hadn't (still fully duplicated, a separate structural question logged in `03-priority-table.md`), but the comparison surfaced that `zenzic-action.md`'s own row for Exit 2 said "Z201 CREDENTIAL_SECRET detected" only, while `github-action-internals.md`'s version of the same table correctly lists all three security-breach codes (`Z201`/`Z204`/`Z205`). Core's own Exit Code Contract — confirmed repeatedly this session — maps all three to Exit 2; the wrapper propagates exit codes without remapping, so this was a genuine content gap, not just duplication. Corrected the row to name all three codes. `mkdocs build --strict`, `just check` (98/100, 0 new) clean.
- **`docs/how-to/configure-social-metadata.md` Described a Fictional `[extra] social_image` Config Key and Automatic Frontmatter-to-Meta-Tag Mapping That Material for MkDocs Does Not Have**:
  - Live-verified against Material for MkDocs' own current documentation (`setup/setting-up-social-cards/`) plus a targeted search of its `extrahead`/meta-tags reference: there is no `[extra] social_image` setting, and the theme does not automatically turn frontmatter `title`/`description`/`image`/`keywords` into `<meta property="og:*">` tags. The real mechanism is two genuinely separate pieces — the `social` plugin (`plugins: - social`), which auto-generates card *images* only, and a manual `{% block extrahead %}` template override reading `page.meta`, which is what actually emits the meta tags a shared link needs. Confirmed this project's own site implements neither today (`mkdocs.yml` has no `social` plugin entry; `overrides/main.html`'s real `extrahead` block only adds a GitHub API preconnect hint, no OG tags) — the page's described mechanism wasn't just undocumented upstream, it also isn't what Zenzic's own docs site does. An initial rewrite reproduced the real two-step mechanism in full (plugin config, a working `extrahead` template override) — corrected again on review: Zenzic implements neither piece, so teaching the reader Material's own template mechanics belongs in Material's docs, not Zenzic's. The page now states plainly that Zenzic does not configure or validate social metadata, links to Material's official `setting-up-social-cards` documentation for the real setup mechanics, and covers only Zenzic's actual connection point — `Z405` (`UNUSED_ASSET`) recognizing a social card image referenced via a page's `image:` frontmatter key — plus the `excluded_assets` pattern for SVG design sources that never appear in a rendered page. Also corrected `docs/how-to/index.md`'s card description for this page. `mkdocs build --strict`, `just check` (98/100, 0 new) clean.

### Known Limitations

- **CLI/LSP Topology Model Divergence**: the CLI's `check_all` pipeline and the Language Server's `IncrementalAnalysisEngine` do not share a common analysis primitive. Most steps (file discovery, rule engine construction/execution, config loading, adapter resolution) genuinely are shared; the two areas that are not are per-file content caching within a single CLI run (partially addressed this release — see below) and, more significantly, orphan/topology detection: the CLI's `Z402` (nav-membership-based) and the LSP's `Z410`/`Z411` (VSM-graph-reachability-based) are two independent algorithms for related-but-not-identical concepts. Formally tracked as an open architectural decision, not silently accepted — see the forthcoming ADR in `docs/developers/explanation/adr-vault/`.
  - This release's caching fix: `_to_findings` no longer re-reads a file's content twice within a single call when that file appears in both `snippet_errors` and `reference_reports`. This addresses only the redundant read *inside* `_to_findings` — the eight independent sub-checks in `_collect_all_results` (`find_orphans`, `find_unused_assets`, `validate_snippets`, etc.) still walk and read files independently of each other; deduplicating across those would require `scanner.py`/`validator.py` to expose raw file content on their result objects, which is a larger change than this release's scope.
- **`check <file>` Is Still Not Near-Instant**: `zenzic check all <file>` now skips the rule-engine pass on non-target files (~18-26% faster overall; ~90% of the rule engine's own cost eliminated); full VSM construction and Pass 1-3 security/topology scanning still run project-wide by design, so overall time remains proportional to project size, not target-file size. True near-instant single-file checking requires a persistent process (see the LSP) — this is not planned for the CLI without a broader architectural change, tracked alongside the CLI/LSP topology-model divergence noted above as the same underlying gap: no shared, persistent analysis primitive between the two.

## Historical Releases

- v0.30.x archive: [changelogs/v0.30.x.md](./changelogs/v0.30.x.md)
- v0.29.x archive: [changelogs/v0.29.x.md](./changelogs/v0.29.x.md)
- v0.28.x archive: [changelogs/v0.28.x.md](./changelogs/v0.28.x.md)
- v0.27.x archive: [changelogs/v0.27.x.md](./changelogs/v0.27.x.md)
- v0.26.x archive: [changelogs/v0.26.x.md](./changelogs/v0.26.x.md)
- v0.25.x archive: [changelogs/v0.25.x.md](./changelogs/v0.25.x.md)
- v0.24.x archive: [changelogs/v0.24.x.md](./changelogs/v0.24.x.md)
- v0.23.x archive: [changelogs/v0.23.x.md](./changelogs/v0.23.x.md)
- v0.22.x archive: [changelogs/v0.22.x.md](./changelogs/v0.22.x.md)
- v0.21.x archive: [changelogs/v0.21.x.md](./changelogs/v0.21.x.md)
- v0.20.x archive: [changelogs/v0.20.x.md](./changelogs/v0.20.x.md)
- v0.19.x archive: [changelogs/v0.19.x.md](./changelogs/v0.19.x.md)
- v0.18.x archive: [changelogs/v0.18.x.md](./changelogs/v0.18.x.md)
- v0.17.x archive: [changelogs/v0.17.x.md](./changelogs/v0.17.x.md)
- v0.16.x archive: [changelogs/v0.16.x.md](./changelogs/v0.16.x.md)
- v0.15.x archive: [changelogs/v0.15.x.md](./changelogs/v0.15.x.md)
- v0.14.x archive: [changelogs/v0.14.md](./changelogs/v0.14.md)
- v0.13.x archive: [changelogs/v0.13.md](./changelogs/v0.13.md)
- v0.12.x archive: [changelogs/v0.12.md](./changelogs/v0.12.md)
- v0.11.x archive: [changelogs/v0.11.md](./changelogs/v0.11.md)
- v0.10.x archive: [changelogs/v0.10.md](./changelogs/v0.10.md)
- v0.9.x archive: [changelogs/v0.9.md](./changelogs/v0.9.md)
- v0.8.x archive: [changelogs/v0.8.md](./changelogs/v0.8.md)
- v0.1.x–v0.7.x archive index: [changelogs/README.md](./changelogs/README.md)
