---
title: "Z511 — Excessive Sentence Length"
---

<!-- SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Z511: EXCESSIVE_SENTENCE_LENGTH

<div class="grid cards" markdown>

- :material-alert-outline:{ .lg .middle style="color: var(--zz-warning);" } **Severity: Warning**

    ---

    Penalty: **1.0 points** | Category: **`content`**

- :material-cog-sync-outline:{ .lg .middle style="color: var(--zz-terminal-meta);" } **Remediation & Opt-In**

    ---

    Auto-Fixable: **No** | Opt-In: **Yes**

</div>

---

## Description

A prose sentence exceeds the configured maximum word count threshold (default: 40 words).

Extremely long sentences reduce reader comprehension, increase cognitive load, and hurt technical documentation readability.

---

## How to Fix

1. Break the sentence into two or more shorter sentences using punctuation (`.`, `;`).
2. Use bullet points or lists to structure complex multi-part statements.

---

## Configuration

`Z511` is opt-in. Enable it, and optionally set the maximum sentence length, in `.zenzic.toml`:

```toml title=".zenzic.toml"
max_sentence_length = 35

[policies]
enable_sentence_length_check = true
```

```toml title="pyproject.toml"
[tool.zenzic]
max_sentence_length = 35

[tool.zenzic.policies]
enable_sentence_length_check = true
```

---

## Suppression

To suppress this warning for a specific sentence, add an inline comment directive at the end of the offending line:

The directive belongs **at the end of the line the finding is on**. Placed on the
line above it suppresses nothing and is itself reported as
[`Z603 DEAD_SUPPRESSION`](Z603.md). MDX files accept the JSX comment form as well,
and the two behave identically — same placement, same effect:

```markdown title="Markdown"
<!-- zenzic:ignore:Z511 -->
```

```markdown title="MDX"
{/* zenzic:ignore:Z511 */}
```

---

## Example

This is the fixture `zenzic lab z511` runs, verbatim. The test suite
executes it and requires `Z511` in the output, so it cannot drift from the
engine without failing.

<div class="grid cards zz-pattern-pair" markdown>

- :material-close-circle:{ .lg .middle style="color: var(--zz-error);" } **Failing Pattern (Triggers Z511)**

    ---

    ```markdown title="docs/index.md"
    # Excessive Sentence Length Example

    This is an exceptionally long prose sentence that continues through multiple clauses and independent ideas without any sentence-ending punctuation until it comfortably exceeds the maximum readability limit of forty words that the Zenzic workspace configuration file defines for any single sentence appearing on this page.

    [Back to main page](index.md)
    ```

</div>

---

## Reference

See the [Finding Codes Index](../reference/finding-codes.md#z511) for finding code details.
