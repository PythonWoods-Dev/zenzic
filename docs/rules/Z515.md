---
title: "Z515 — Bare URL Used"
---

<!-- SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Z515: BARE_URL_USED

<div class="grid cards" markdown>

- :material-alert-outline:{ .lg .middle style="color: var(--zz-warning);" } **Severity: Warning**

    ---

    Penalty: **1.0 points** | Category: **`content`**

- :material-cog-sync-outline:{ .lg .middle style="color: var(--zz-terminal-meta);" } **Remediation & Opt-In**

    ---

    Auto-Fixable: **Yes** | Opt-In: **No**

</div>

---

## Description

A raw HTTP or HTTPS URL appears directly in prose without being enclosed in angle brackets (`<url>`) or formatted as a Markdown link (`[text](url)`).

Bare URLs can render inconsistently across Markdown parsers and reduce readability.

---

## How to Fix

Wrap the URL in angle brackets or format it as a descriptive Markdown link:

```markdown
Visit <https://example.com> for more info.
```

Run `zenzic fix --apply` to automatically wrap bare URLs across your documentation.

---

## Suppression

Suppress with an inline comment:

The directive belongs **at the end of the line the finding is on**. Placed on the
line above it suppresses nothing and is itself reported as
[`Z603 DEAD_SUPPRESSION`](Z603.md). MDX files accept the JSX comment form as well,
and the two behave identically — same placement, same effect:

```markdown title="Markdown"
<!-- zenzic:ignore:Z515 -->
```

```markdown title="MDX"
{/* zenzic:ignore:Z515 */}
```

---

## Example

This is the fixture `zenzic lab z515` runs, verbatim. The test suite
executes it and requires `Z515` in the output, so it cannot drift from the
engine without failing.

<div class="grid cards zz-pattern-pair" markdown>

- :material-close-circle:{ .lg .middle style="color: var(--zz-error);" } **Failing Pattern (Triggers Z515)**

    ---

    ```markdown title="docs/index.md"
    # Bare URL Example

    Visit https://example.com for more information. Wrapping raw URLs in proper
    Markdown link syntax gives readers a descriptive label instead of a long
    opaque string, and it also lets renderers apply consistent link styling
    across the page rather than leaving the browser's default plain-text
    rendering for that one address.
    ```

</div>

---

## Reference

See the [Finding Codes Index](../reference/finding-codes.md#z515) for finding code details.
