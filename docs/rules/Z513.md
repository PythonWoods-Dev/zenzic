---
title: "Z513 — Duplicate Heading"
---

<!-- SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Z513: DUPLICATE_HEADING

<div class="grid cards" markdown>

- :material-alert-outline:{ .lg .middle style="color: #f59e0b;" } **Severity: Warning**

    ---

    Penalty: **2.0 points** | Category: **`content`**

- :material-cog-sync-outline:{ .lg .middle style="color: #64748b;" } **Remediation & Opt-In**

    ---

    Auto-Fixable: **No** | Opt-In: **Yes**

</div>

---

## Description

Two or more headings in the same document resolve to identical text (ignoring case and whitespace).

Duplicate headings create ambiguous slug anchors (e.g. `#section` vs `#section-1`), breaking deep links and confusing document outline navigation.

---

## How to Fix

Rename headings so that every section has a unique, descriptive title.

---

## Configuration

`Z513` is opt-in. Enable it under `[policies]`:

```toml title=".zenzic.toml"
[policies]
enable_duplicate_heading_check = true
```

```toml title="pyproject.toml"
[tool.zenzic.policies]
enable_duplicate_heading_check = true
```

---

## Suppression

If duplicate headings are required by design, suppress the warning with an inline comment:

The directive belongs **at the end of the line the finding is on**. Placed on the
line above it suppresses nothing and is itself reported as
[`Z603 DEAD_SUPPRESSION`](Z603.md). MDX files accept the JSX comment form as well,
and the two behave identically — same placement, same effect:

```markdown title="Markdown"
<!-- zenzic:ignore:Z513 -->
```

```markdown title="MDX"
{/* zenzic:ignore:Z513 */}
```

---

## Example

This is the fixture `zenzic lab z513` runs, verbatim. The test suite
executes it and requires `Z513` in the output, so it cannot drift from the
engine without failing.

<div class="grid cards zz-pattern-pair" markdown>

- :material-close-circle:{ .lg .middle style="color: #ef4444;" } **Failing Pattern (Triggers Z513)**

    ---

    ```markdown title="docs/index.md"
    # Duplicate Heading Example

    ## Configuration

    Section about configuration.

    ## Configuration

    Duplicate section about configuration. Repeating an identical heading text
    twice in one document confuses anchor links and table-of-contents tooling,
    since both sections resolve to the same generated fragment identifier and
    a reader following an in-page anchor can only ever land on the first one,
    never the second.
    ```

</div>

---

## Reference

See the [Finding Codes Index](../reference/finding-codes.md#z513) for finding code details.
