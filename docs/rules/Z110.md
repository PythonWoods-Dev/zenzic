---
title: "Z110 — Config Syntax Error"
---

<!-- SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Z110: CONFIG_SYNTAX_ERROR

<div class="grid cards" markdown>

- :material-alert-circle:{ .lg .middle style="color: var(--zz-error);" } **Severity: Error**

    ---

    Penalty: **0.0 points** | Category: **`general`**

- :material-cog-sync-outline:{ .lg .middle style="color: var(--zz-terminal-meta);" } **Remediation & Opt-In**

    ---

    Auto-Fixable: **No** | Opt-In: **No**

</div>

---

## Description

The `.zenzic.toml` (or `pyproject.toml`) workspace configuration file contains a malformed TOML syntax error.

When a configuration file is syntactically invalid, Zenzic halts Markdown document analysis to prevent cascading false positives or running with corrupted governance policies.

### In the editor, it is a finding rather than a halt

`zenzic check`, `audit` and `guard` stop and exit `1`. The language server cannot: a session with
no diagnostics on screen is indistinguishable from a clean document, and before v0.31.0 that is
exactly what happened — an unparseable `pyproject.toml` made the editor publish **nothing at
all**, credential findings included.

So the editor keeps the built-in defaults, keeps analysing, and reports this code against the
file that will not parse. The diagnostic is also the warning that what you see is not what CI
will report: your `docs_dir`, your policies and your `forbidden_patterns` are **not** in effect
while the file is broken.

This holds whether the file was already broken when the editor opened it or is broken while you
edit — the configuration is re-read on change, and the same answer is given.

---

## How to Fix

Fix the TOML syntax error in `.zenzic.toml` indicated in the diagnostic message (such as missing quotes, unclosed brackets, or unescaped characters).

---

## Example Error

```text
# Malformed TOML
placeholder_max_words = [ 50,
```

Corrected `.zenzic.toml`:

```toml
placeholder_max_words = 50
```

---

## Why there is no runnable example

`Z110` is a **configuration-load error** — malformed TOML — on the same path as
[`Z001`](Z001.md). It is raised while the file is parsed, before any Markdown is
read and before a findings payload exists, so the run aborts instead of
reporting. An executable card example requires output to assert against, and
there is none.

A second reason applies to this code specifically: its error panel prints the
**absolute path of the machine that ran it**, so a captured block would encode
one developer's filesystem. The gallery page for this code is deliberately left
as it is for the same reason.

---

## Reference

See the [Finding Codes Index](../reference/finding-codes.md#z110) for finding code details.
