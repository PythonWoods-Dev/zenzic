---
title: "Z618 — Required Heading Pattern"
---

<!-- SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Z618: REQUIRED_HEADING_PATTERN

<div class="grid cards" markdown>

- :material-alert-outline:{ .lg .middle style="color: var(--zz-warning);" } **Severity: Warning**

    ---

    Penalty: **3.0 points** | Category: **`brand`**

- :material-cog-sync-outline:{ .lg .middle style="color: var(--zz-terminal-meta);" } **Remediation & Opt-In**

    ---

    Auto-Fixable: **No** | Opt-In: **Yes**

</div>

---

## Description

A document does not contain any heading matching a required regular expression pattern declared in `[policies].required_heading_patterns`.

This policy enforces standardized document templates and structure (for example, ensuring that every guide includes an `Overview` or `Prerequisites` section).

---

## How to Fix

Add the missing section heading matching the required pattern to the document.

---

## Configuration

Declare required heading patterns in `.zenzic.toml`:

```toml
[policies]
required_heading_patterns = ["^Overview$", "^Prerequisites$"]
```

---

## Suppression

Suppress with an inline comment:

The directive belongs **at the end of the line the finding is on**. Placed on the
line above it suppresses nothing and is itself reported as
[`Z603 DEAD_SUPPRESSION`](Z603.md). MDX files accept the JSX comment form as well,
and the two behave identically — same placement, same effect:

```markdown title="Markdown"
<!-- zenzic:ignore:Z618 -->
```

```markdown title="MDX"
{/* zenzic:ignore:Z618 */}
```

---

## Example

This is the fixture `zenzic lab z618` runs, verbatim. The test suite
executes it and requires `Z618` in the output, so it cannot drift from the
engine without failing.

<div class="grid cards zz-pattern-pair" markdown>

- :material-close-circle:{ .lg .middle style="color: var(--zz-error);" } **Failing Pattern (Triggers Z618)**

    ---

    ```markdown title="docs/index.md"
    # Installation Guide

    This document lacks an Overview heading. Requiring a standard heading like "Overview" across every page in a documentation set gives readers a predictable entry point regardless of which specific page they land on first. That holds whether they arrived via search or a direct shared link from a colleague.
    ```

</div>

---

## Reference

See the [Finding Codes Index](../reference/finding-codes.md#z618) for finding code details.
