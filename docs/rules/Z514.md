---
title: "Z514 — Generic Image Alt Text"
---

<!-- SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev> -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Z514: GENERIC_IMAGE_ALT_TEXT

<div class="grid cards" markdown>

- :material-alert-outline:{ .lg .middle style="color: #f59e0b;" } **Severity: Warning**

    ---

    Penalty: **2.0 points** | Category: **`content`**

- :material-cog-sync-outline:{ .lg .middle style="color: #64748b;" } **Remediation & Opt-In**

    ---

    Auto-Fixable: **No** | Opt-In: **No**

</div>

---

## Description

An image tag (`![]()` or `<img>`) uses a generic filler word (such as "image", "screenshot", "picture", "photo", "icon", or "logo") as its alternative text.

Generic alt text fails accessibility requirements by conveying no meaningful information to users who rely on screen readers.

---

## How to Fix

Provide concise, descriptive alternative text that explains what the image communicates in context.

---

## Suppression

If generic alt text cannot be avoided, suppress the warning with an inline comment:

The directive belongs **at the end of the line the finding is on**. Placed on the
line above it suppresses nothing and is itself reported as
[`Z603 DEAD_SUPPRESSION`](Z603.md). MDX files accept the JSX comment form as well,
and the two behave identically — same placement, same effect:

```markdown title="Markdown"
<!-- zenzic:ignore:Z514 -->
```

```markdown title="MDX"
{/* zenzic:ignore:Z514 */}
```

---

## Example

This is the fixture `zenzic lab z514` runs, verbatim. The test suite
executes it and requires `Z514` in the output, so it cannot drift from the
engine without failing.

<div class="grid cards zz-pattern-pair" markdown>

- :material-close-circle:{ .lg .middle style="color: #ef4444;" } **Failing Pattern (Triggers Z514)**

    ---

    ```markdown title="docs/index.md"
    # Generic Alt Text Example

    ![image](assets/logo.png)

    This page triggers Z514 due to generic alt text. Descriptive alt text lets
    screen readers convey what an image actually shows, instead of a filler word
    that tells assistive technology nothing about the image's content or purpose.
    See the [WCAG non-text content guideline](https://www.w3.org/WAI/WCAG22/Understanding/non-text-content.html)
    for the full accessibility rationale.
    ```

</div>

---

## Reference

See the [Finding Codes Index](../reference/finding-codes.md#z514) for finding code details.
